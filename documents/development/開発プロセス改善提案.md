# 開発プロセス改善提案

**作成日**: 2025-12-31
**目的**: AI駆動開発における品質担保と効率化の最適化

---

## 📊 現状分析

### 1. ドキュメントと実態のギャップ

| 項目 | ドキュメント記載 | 実際の状況 | ギャップ |
|------|----------------|-----------|---------|
| **開発フロー** | Issue → Gherkin → E2E → Unit → 実装 | 実装 → テスト（推測） | ⚠️ **大** |
| **Gherkinシナリオ** | BDD必須 | .featureファイル0件 | ⚠️ **大** |
| **E2Eテスト** | 主要フロー100% | 4ファイルのみ（booking未実装） | ⚠️ **中** |
| **テストカバレッジ** | 80%目標 | 測定データなし | ⚠️ **中** |
| **テストファースト** | TDD推奨 | 後付けテストの可能性 | ⚠️ **大** |

### 2. コード品質の現状

**✅ 良い点:**
- TypeScript strict mode使用
- Zodバリデーション導入
- E2EテストにGiven/When/Thenコメント記載
- PR前チェックリストが詳細
- 日本語コミットメッセージの統一

**⚠️ 改善が必要な点:**
- テストが実装の後に追加されている可能性
- Gherkinシナリオが実際には作成されていない
- テストカバレッジの可視化がない
- リファクタ時のテスト修正が発生する可能性

### 3. 指摘された問題点の検証

#### ❌ 問題1: テストが後付けになっている

**証拠:**
```bash
# E2Eテストの状況
admin-dashboard.spec.ts  ✅ 存在
auth.spec.ts             ✅ 存在
api.spec.ts              ✅ 存在
home.spec.ts             ✅ 存在
booking.spec.ts          ❌ 未実装（#8-11のコア機能）
```

**影響:**
- booking機能は実装済みだがE2Eテストが未実装
- テスタビリティを考慮せずに実装された可能性
- リファクタ時のリグレッション検出が困難

#### ❌ 問題2: Gherkinシナリオが実際には作成されていない

**証拠:**
```bash
$ find . -name "*.feature" -o -name "*.gherkin"
# 結果: 0件
```

**影響:**
- Living Documentation が機能していない
- 仕様とコードの乖離リスク
- ステークホルダーとの共通言語がない

#### ⚠️ 問題3: テスト修正の必要性（リファクタ時）

**証拠不足だが懸念あり:**
- E2Eテストが実装詳細（セレクタ、要素名）に依存している可能性
- 例: `page.click('button:has-text("アカウントを作成")')` はテキスト変更で壊れる

---

## 🎯 推奨する改善アプローチ

### Option A: **スペックファースト（推奨）**

AI駆動開発に最適化したプロセス。

#### フロー

```
1. [人間+AI] ユーザーストーリー → Gherkinシナリオ作成
   ↓
2. [AI] GherkinからE2Eテストコード生成
   ↓
3. [AI] E2Eテストを実行（Red確認）
   ↓
4. [AI] 必要な単体テストを生成
   ↓
5. [AI] テストをパスする最小実装
   ↓
6. [人間] レビュー → リファクタ指示
   ↓
7. [AI] リファクタ実装（テストは変更しない）
   ↓
8. [自動] CI/CD実行 → Green確認
```

#### 具体例: 予約機能の実装

```gherkin
# features/booking.feature
Feature: 予約機能
  As a customer
  I want to book a reservation
  So that I can visit the store at my preferred time

  Scenario: 予約成功（ハッピーパス）
    Given ユーザー"山田太郎"がログインしている
    And 予約カレンダーページにアクセスしている
    When "2025年1月20日"を選択する
    And "14:00"の時間帯を選択する
    And "カット"メニューを選択する
    And "田中"スタッフを選択する
    And "予約を確定する"ボタンをクリックする
    Then 予約完了画面が表示される
    And 予約IDが発行される
    And 確認メールが"yamada@example.com"に送信される

  Scenario: 予約失敗（時間帯が埋まっている）
    Given ユーザーがログインしている
    And "2025年1月20日 14:00"の時間帯が埋まっている
    When 同じ時間帯で予約を試みる
    Then エラーメッセージ"この時間は既に予約済みです"が表示される
    And 予約は作成されない

  Scenario: 予約失敗（過去の日付）
    Given ユーザーがログインしている
    When 過去の日付で予約を試みる
    Then エラーメッセージ"過去の日付は予約できません"が表示される
```

#### AIへのプロンプト例

```
【ステップ1】
以下のGherkinシナリオに対応するPlaywright E2Eテストを生成してください。
- Page Objectパターンを使用
- Given/When/Thenコメントを含める
- データテストIDを使用（テキストに依存しない）
[Gherkinシナリオを貼り付け]

【ステップ2】
以下のE2Eテストをパスする最小実装を生成してください。
- Next.js App Router使用
- TypeScript strict mode
- Zodバリデーション
[E2Eテストコードを貼り付け]

【ステップ3】
以下のコードをリファクタリングしてください。
観点:
- DRY原則
- 単一責任の原則
- 関数は小さく（10行以内）
注意: E2Eテストは変更しないこと
[実装コードを貼り付け]
```

---

### Option B: **契約駆動（Contract-First）**

API中心のアプローチ。バックエンドとフロントエンドの並行開発に有効。

#### フロー

```
1. OpenAPI/GraphQL スキーマ定義
   ↓
2. スキーマから型定義自動生成
   ↓
3. スキーマからモックサーバー起動（MSW等）
   ↓
4. モックを使ってフロントエンド実装
   ↓
5. 契約テスト実行（Pact等）
   ↓
6. バックエンド実装
```

#### 具体例: 予約API

```yaml
# openapi.yaml
paths:
  /api/reservations:
    post:
      summary: 予約作成
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateReservationRequest'
      responses:
        '201':
          description: 予約作成成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReservationResponse'
        '400':
          description: バリデーションエラー
        '409':
          description: 時間帯が既に予約済み

components:
  schemas:
    CreateReservationRequest:
      type: object
      required:
        - menuId
        - staffId
        - reservedDate
        - reservedTime
      properties:
        menuId:
          type: string
          format: uuid
        staffId:
          type: string
          format: uuid
        reservedDate:
          type: string
          format: date
        reservedTime:
          type: string
          pattern: '^([0-1][0-9]|2[0-3]):[0-5][0-9]$'
        notes:
          type: string
          maxLength: 500
```

#### ツール導入

```bash
# OpenAPI型生成
npm install -D openapi-typescript
npx openapi-typescript openapi.yaml -o src/types/api.ts

# MSWモック生成
npm install -D msw
npx msw init public/ --save

# 契約テスト
npm install -D @pact-foundation/pact
```

---

### Option C: **プロパティベーステスト併用**

エッジケースの網羅性を向上。

#### 導入

```bash
npm install -D fast-check
```

#### 例: バリデーションのプロパティテスト

```typescript
import fc from 'fast-check';
import { createReservationSchema } from '@/lib/validations';

describe('createReservationSchema - Property-Based Tests', () => {
  it('should accept valid UUID for menuId', () => {
    fc.assert(
      fc.property(fc.uuid(), (menuId) => {
        const data = {
          menuId,
          staffId: '123e4567-e89b-12d3-a456-426614174001',
          reservedDate: '2025-12-31',
          reservedTime: '14:00',
        };
        const result = createReservationSchema.safeParse(data);
        expect(result.success).toBe(true);
      })
    );
  });

  it('should reject invalid date formats', () => {
    fc.assert(
      fc.property(
        fc.string().filter(s => !/^\d{4}-\d{2}-\d{2}$/.test(s)),
        (invalidDate) => {
          const data = {
            menuId: '123e4567-e89b-12d3-a456-426614174000',
            staffId: '123e4567-e89b-12d3-a456-426614174001',
            reservedDate: invalidDate,
            reservedTime: '14:00',
          };
          const result = createReservationSchema.safeParse(data);
          expect(result.success).toBe(false);
        }
      )
    );
  });
});
```

---

## 🛠️ 実践的なワークフロー（推奨）

### フェーズ1: 基盤整備（Week 1）

#### 1. Gherkinシナリオの導入

```bash
# ディレクトリ作成
mkdir -p features/{auth,booking,admin}

# Cucumberインストール（オプション）
npm install -D @cucumber/cucumber
```

**成果物:**
```
features/
├── auth/
│   ├── register.feature
│   └── login.feature
├── booking/
│   ├── booking.feature
│   └── calendar.feature
└── admin/
    ├── dashboard.feature
    └── reservations.feature
```

#### 2. テストカバレッジの可視化

```bash
# package.json に追加
{
  "scripts": {
    "test:coverage": "jest --coverage",
    "test:coverage:watch": "jest --coverage --watch"
  }
}

# jest.config.js に追加
module.exports = {
  collectCoverageFrom: [
    'src/**/*.{ts,tsx}',
    '!src/**/*.d.ts',
    '!src/**/*.stories.tsx',
    '!src/**/__tests__/**'
  ],
  coverageThresholds: {
    global: {
      branches: 70,
      functions: 70,
      lines: 80,
      statements: 80
    }
  }
};
```

#### 3. CI/CDでカバレッジレポート

```yaml
# .github/workflows/test.yml
- name: Run tests with coverage
  run: npm run test:coverage

- name: Upload coverage to Codecov
  uses: codecov/codecov-action@v3
  with:
    files: ./coverage/coverage-final.json
```

#### 4. Page Objectパターンの導入

```typescript
// src/__tests__/e2e/pages/BookingPage.ts
export class BookingPage {
  constructor(private page: Page) {}

  // セレクタを一元管理
  private selectors = {
    dateButton: (day: string) => `[data-testid="date-${day}"]`,
    timeSlot: (time: string) => `[data-testid="time-${time}"]`,
    menuSelect: '[data-testid="menu-select"]',
    staffSelect: '[data-testid="staff-select"]',
    submitButton: '[data-testid="submit-booking"]',
    successMessage: '[data-testid="success-message"]',
  };

  async selectDate(day: string) {
    await this.page.click(this.selectors.dateButton(day));
  }

  async selectTime(time: string) {
    await this.page.click(this.selectors.timeSlot(time));
  }

  async selectMenu(menuName: string) {
    await this.page.selectOption(this.selectors.menuSelect, menuName);
  }

  async selectStaff(staffName: string) {
    await this.page.selectOption(this.selectors.staffSelect, staffName);
  }

  async submit() {
    await this.page.click(this.selectors.submitButton);
  }

  async expectSuccessMessage(message: string) {
    const element = this.page.locator(this.selectors.successMessage);
    await expect(element).toContainText(message);
  }
}
```

**メリット:**
- テキスト変更に強い（data-testid使用）
- テストコードの可読性向上
- セレクタ変更時の修正が1箇所で済む

---

### フェーズ2: 開発サイクルの改善（Week 2-4）

#### 毎日のルーチン

```
【朝】
1. 今日のIssueを確認
2. Gherkinシナリオを書く（AIと協働）
3. AIにE2Eテスト生成を指示
4. E2Eテストを実行（Red確認）

【開発中】
5. AIに実装を生成させる
6. テストをパス（Green）
7. 人間がレビュー → リファクタ指示
8. AIがリファクタ実装

【終業前】
9. 品質チェック実行
10. コミット・プッシュ
11. PR作成
```

#### 週次ルーチン

```
【金曜午後】
1. 技術的負債のトリアージ
2. コードレビュー
3. リファクタリングタスク作成（1-2個）
4. Sprint Retrospective
```

#### 月次ルーチン

```
【月末】
1. アーキテクチャ健全性チェック
2. 依存関係の更新（npm audit）
3. パフォーマンス測定（Lighthouse）
4. テストカバレッジレポート
```

---

## 📈 品質指標の可視化

### 1. GitHub Actionsバッジ

```markdown
# README.md
![CI](https://github.com/user/repo/workflows/CI/badge.svg)
![Coverage](https://codecov.io/gh/user/repo/branch/main/graph/badge.svg)
![License](https://img.shields.io/badge/license-MIT-blue.svg)
```

### 2. テストカバレッジの追跡

| Sprint | Lines | Branches | Functions | Statements |
|--------|-------|----------|-----------|------------|
| 1      | 65%   | 55%      | 60%       | 65%        |
| 2      | 75%   | 65%      | 70%       | 75%        |
| 3      | 85%   | 75%      | 80%       | 85%        |
| **目標** | **80%** | **70%** | **70%** | **80%** |

### 3. 技術負債の定量化

```typescript
// SonarQubeまたはCodeClimate使用
{
  "codeSmells": 12,
  "technicalDebt": "2h 30m",
  "maintainability": "A",
  "reliability": "A",
  "security": "A"
}
```

---

## ✅ 即座に実施すべき改善（優先度順）

### 🔴 優先度: 最高

1. **booking.spec.ts（E2E）を作成**
   - 現在未実装の最重要テスト
   - Issue #8-11のコア機能をカバー

2. **Gherkinシナリオを書く**
   - 既存機能の振る舞いを文書化
   - 新機能はGherkinから開始

3. **Page Objectパターン導入**
   - テストの保守性向上
   - data-testid属性を追加

### 🟡 優先度: 高

4. **テストカバレッジ測定の自動化**
   - jest --coverage設定
   - CI/CDに組み込み

5. **プロパティベーステスト導入**
   - バリデーションロジックから開始
   - fast-check導入

6. **Living Documentation化**
   - Gherkinから自動生成
   - Cucumberレポート活用

### 🟢 優先度: 中

7. **契約テストの導入**
   - OpenAPIスキーマ定義
   - MSWでモック

8. **技術負債の可視化**
   - SonarCloud連携
   - 週次レポート

---

## 🎯 成功の判断基準

### 3ヶ月後のゴール

- ✅ すべてのIssueにGherkinシナリオが存在
- ✅ E2Eテストカバレッジ: 主要フロー100%
- ✅ 単体テストカバレッジ: 80%以上
- ✅ 技術負債: 1日以内
- ✅ リファクタ時のテスト修正: 0件
- ✅ CI/CD実行時間: 5分以内
- ✅ PRマージまでの時間: 24時間以内

### 品質指標（KPI）

| 指標 | 現在 | 3ヶ月後目標 |
|------|------|------------|
| テストカバレッジ | 不明 | 80% |
| E2Eテスト数 | 4ファイル | 全機能カバー |
| Gherkinシナリオ | 0件 | 全機能 |
| バグ発生率 | 不明 | <5件/Sprint |
| Lighthouseスコア | 不明 | >90 |
| 技術負債時間 | 不明 | <1日 |

---

## 🚀 アクションプラン

### 今週（Week 1）

- [ ] このドキュメントをチーム全員で読む
- [ ] Gherkinシナリオテンプレート作成
- [ ] booking.spec.ts作成（最優先）
- [ ] テストカバレッジ測定開始

### 来週（Week 2）

- [ ] Page Objectパターン導入
- [ ] data-testid属性追加
- [ ] プロパティベーステスト導入
- [ ] CI/CDにカバレッジレポート追加

### 1ヶ月後

- [ ] Living Documentation公開
- [ ] 契約テスト導入
- [ ] 技術負債可視化
- [ ] プロセス振り返り

---

## 📚 参考資料

### 推奨図書

- **BDD in Action** - John Ferguson Smart
- **Growing Object-Oriented Software, Guided by Tests** - Steve Freeman & Nat Pryce
- **Test Driven Development** - Kent Beck

### ツール・ライブラリ

| カテゴリ | ツール | 用途 |
|---------|-------|------|
| BDD | Cucumber.js | Gherkin実行 |
| E2E | Playwright | ブラウザテスト |
| 単体テスト | Jest | ユニットテスト |
| プロパティテスト | fast-check | ランダムテスト |
| 契約テスト | Pact | API契約 |
| カバレッジ | Codecov | 可視化 |
| 静的解析 | SonarCloud | コード品質 |

---

**最終更新**: 2025-12-31
**次回レビュー**: 2026-01-31
