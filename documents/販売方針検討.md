# 販売方針検討 - SaaS型 vs 買い切り型

**最終更新**: 2026-01-01

---

## 📋 目次

1. [現状分析](#現状分析)
2. [販売方針比較](#販売方針比較)
3. [SaaS型の検討事項](#saas型の検討事項)
4. [買い切り型の検討事項](#買い切り型の検討事項)
5. [コード共通化戦略](#コード共通化戦略)
6. [インフラ選定](#インフラ選定)
7. [データベース設計](#データベース設計)
8. [推奨アプローチ](#推奨アプローチ)

---

## 📊 現状分析

### プロジェクトの特徴

- **技術スタック**: Next.js 16 + Supabase + Prisma 7
- **現在のホスティング**: Vercel (Serverless)
- **データベース**: Supabase PostgreSQL
- **マルチテナント**: 既に実装済み（`tenant_id`による論理的分離）
- **認証**: Supabase Auth

### 既存のマルチテナント設計

```typescript
// 全テーブルに tenant_id がある
model RestaurantUser {
  id        String   @id @default(uuid())
  tenantId  String   @default("demo-restaurant")
  email     String
  ...
}

// 環境変数で制御
const TENANT_ID = process.env.NEXT_PUBLIC_TENANT_ID || 'demo-restaurant';
```

**メリット**:
- ✅ 既にマルチテナント対応済み
- ✅ インフラコストの削減が可能
- ✅ 複数顧客を1つのDBで管理可能

---

## 🔀 販売方針比較

| 項目 | SaaS型 | 買い切り型 |
|-----|--------|-----------|
| **価格モデル** | 月額/年額課金 | 初期費用のみ |
| **顧客の所有感** | 低い（サービス利用） | 高い（自社システム） |
| **保守責任** | 提供者側 | 購入者側または有償契約 |
| **インフラコスト** | 提供者負担（従量課金） | 購入者負担 |
| **カスタマイズ** | 制限あり（共通機能のみ） | 自由（コード納品） |
| **アップデート** | 自動（全顧客に適用） | 手動（個別対応） |
| **初期コスト** | 低い | 高い |
| **LTV（顧客生涯価値）** | 高い（継続収益） | 低い（1回きり） |
| **セキュリティ** | 提供者が管理 | 購入者が管理 |
| **スケーラビリティ** | 高い | 中〜低い |

### 需要の想定

#### SaaS型が向いている顧客
- 小規模店舗（1〜2店舗）
- ITリソースがない
- 初期コストを抑えたい
- 保守を任せたい

#### 買い切り型が向いている顧客
- 中〜大規模店舗（3店舗以上）
- 自社でインフラ管理できる
- カスタマイズしたい
- データを自社で完全管理したい

---

## 🌐 SaaS型の検討事項

### 1. ビジネスモデル

#### 価格設定例
```
ライトプラン:     月額 5,000円  / 予約数100件まで / スタッフ3人まで
スタンダードプラン: 月額 10,000円 / 予約数500件まで / スタッフ10人まで
プレミアムプラン:  月額 20,000円 / 予約数無制限  / スタッフ無制限 + カスタムドメイン
```

#### 収益予測（10顧客の場合）
- ライト 5顧客 × 5,000円 = 25,000円/月
- スタンダード 3顧客 × 10,000円 = 30,000円/月
- プレミアム 2顧客 × 20,000円 = 40,000円/月
- **合計: 95,000円/月（114万円/年）**

### 2. インフラ構成（マルチテナントSaaS）

#### 推奨構成
```
┌─────────────────────────────────────────────┐
│              顧客A                           │
│         (tenant_id: 'customer-a')           │
└─────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────┐
│          Vercel (Next.js App)               │
│  - 全顧客共通のアプリケーション                │
│  - テナントIDで動的ルーティング                │
│  - 例: customer-a.yourdomain.com             │
└─────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────┐
│      Supabase PostgreSQL (共有DB)           │
│  - 全顧客のデータを1つのDBで管理              │
│  - tenant_id で論理的に分離                  │
│  - Row Level Security (RLS) で保護          │
└─────────────────────────────────────────────┘
```

#### コスト試算（月額）
| 項目 | サービス | 想定コスト（10顧客） |
|-----|---------|-------------------|
| ホスティング | Vercel Pro | $20/月 (約3,000円) |
| データベース | Supabase Pro | $25/月 (約3,750円) |
| メール送信 | Resend | $10/月 (約1,500円) |
| ドメイン | CloudFlare | $10/月 (約1,500円) |
| **合計** | - | **約9,750円/月** |

**利益率**: (95,000 - 9,750) / 95,000 = **約90%**

### 3. セキュリティ要件

#### Row Level Security (RLS) の実装

```sql
-- ユーザーは自身のテナントのデータのみアクセス可能
CREATE POLICY tenant_isolation ON restaurant_users
  USING (tenant_id = current_setting('app.current_tenant'));

-- 管理者は全テナント閲覧可能（運用用）
CREATE POLICY admin_access ON restaurant_users
  USING (
    current_setting('app.user_role') = 'admin' OR
    tenant_id = current_setting('app.current_tenant')
  );
```

#### データ分離の徹底
```typescript
// ミドルウェアでテナントIDを設定
export function middleware(request: NextRequest) {
  // サブドメインからテナントIDを抽出
  const hostname = request.headers.get('host') || '';
  const tenantId = hostname.split('.')[0]; // customer-a.yourdomain.com → customer-a

  // リクエストヘッダーに追加
  const requestHeaders = new Headers(request.headers);
  requestHeaders.set('x-tenant-id', tenantId);

  return NextResponse.next({
    request: { headers: requestHeaders },
  });
}

// APIルートで使用
export async function GET(request: Request) {
  const tenantId = request.headers.get('x-tenant-id');

  const reservations = await prisma.restaurantReservation.findMany({
    where: { tenantId },
  });

  return Response.json(reservations);
}
```

### 4. スケーラビリティ

#### 顧客数に応じた対応

| 顧客数 | データベース戦略 | 推奨構成 |
|--------|---------------|---------|
| 1-50 | 単一DB（論理分離） | Supabase Pro |
| 51-500 | 単一DB + Read Replica | Supabase Pro + レプリカ |
| 501+ | シャーディング検討 | 複数DB + ルーティング層 |

#### パフォーマンス対策
- インデックス最適化（`tenant_id`を複合インデックスの先頭に）
- コネクションプーリング（PgBouncer）
- クエリキャッシング（Redis）

### 5. データバックアップ

#### Supabaseの標準機能
- 自動バックアップ（Daily）: Supabase Proプラン標準
- Point-in-Time Recovery (PITR): 7日間

#### 追加バックアップ戦略
```bash
# テナント別バックアップスクリプト
pg_dump --host=db.xxx.supabase.co \
        --port=5432 \
        --username=postgres \
        --table=restaurant_users \
        --table=restaurant_reservations \
        --table=restaurant_menus \
        --where="tenant_id = 'customer-a'" \
        > backup-customer-a-$(date +%Y%m%d).sql
```

### 6. 運用・保守

#### 必要な体制
- **開発・保守**: 月10-20時間（機能追加・バグ修正）
- **監視**: Vercel Analytics + Supabase Logs
- **サポート**: メール・チャット対応（営業時間内）

#### 監視項目
- アプリケーションエラー率
- データベースレスポンスタイム
- APIエンドポイント可用性
- ディスク使用率

---

## 💾 買い切り型の検討事項

### 1. 販売モデル

#### 価格設定例
```
基本パッケージ:   300,000円（ソースコード + セットアップ支援）
カスタマイズ版:   500,000円〜（機能追加・UI変更込み）
保守契約（オプション）: 50,000円/年（バグ修正・セキュリティパッチ）
```

### 2. インフラ構成（買い切り型）

#### パターンA: 自社VPS（完全独立）

```
┌─────────────────────────────────────────────┐
│         顧客A専用環境                         │
│                                             │
│  ┌───────────────────────────────────────┐  │
│  │  VPS (ConoHa / さくら / AWS Lightsail) │  │
│  │  - Next.jsアプリ (Docker)                │  │
│  │  - PostgreSQL (Docker)                  │  │
│  │  - Nginx (リバースプロキシ)               │  │
│  └───────────────────────────────────────┘  │
│                                             │
│  独自ドメイン: customer-a.com                │
└─────────────────────────────────────────────┘
```

**顧客のコスト（月額）**:
- VPS: 2,000〜5,000円/月（2GB〜4GBメモリ）
- ドメイン: 100円/月
- SSL証明書: 無料（Let's Encrypt）
- **合計: 2,100〜5,100円/月**

#### パターンB: Vercel + Supabase（簡易構成）

```
┌─────────────────────────────────────────────┐
│         顧客A専用環境                         │
│                                             │
│  ┌───────────────────────────────────────┐  │
│  │  Vercel (Hobby/Pro)                    │  │
│  │  - Next.jsアプリ                         │  │
│  └───────────────────────────────────────┘  │
│                │                            │
│                ▼                            │
│  ┌───────────────────────────────────────┐  │
│  │  Supabase (Free/Pro)                   │  │
│  │  - PostgreSQL                           │  │
│  │  - Auth                                 │  │
│  └───────────────────────────────────────┘  │
│                                             │
│  独自ドメイン: customer-a.com                │
└─────────────────────────────────────────────┘
```

**顧客のコスト（月額）**:
- Vercel Hobby: 無料（商用利用は$20/月）
- Supabase Free: 無料（Pro: $25/月）
- **合計: 0円〜6,750円/月**

### 3. 納品物

#### 基本パッケージ内容
1. **ソースコード一式**
   - `reserve-app/` ディレクトリ全体
   - `.env.example` テンプレート
   - `docker-compose.yml`（VPS版の場合）

2. **ドキュメント**
   - セットアップガイド
   - 運用マニュアル
   - API仕様書
   - DB設計書

3. **初期設定支援**
   - 環境構築サポート（1-2時間のオンライン対応）
   - 初期データ投入

#### カスタマイズ版（追加料金）
- UI/UXのカスタマイズ
- 機能追加（例: LINE連携、Instagram連携）
- 独自決済システム統合
- 多店舗展開対応

### 4. サポート体制

#### 初期サポート（納品後30日間）
- 無料バグ修正
- 環境設定支援
- 基本的な操作説明

#### 保守契約（オプション）
- **年額50,000円**: バグ修正・セキュリティパッチ提供
- **年額100,000円**: 上記 + 小規模機能追加（月5時間まで）

### 5. データ管理

#### バックアップ方針（顧客側の責任）

**VPS版**:
```bash
# 自動バックアップスクリプト（cron設定）
#!/bin/bash
# 毎日3時にDBバックアップ
0 3 * * * docker exec postgres pg_dump -U postgres reserve_db > /backup/db-$(date +\%Y\%m\%d).sql

# 14日以上古いバックアップを削除
find /backup -name "db-*.sql" -mtime +14 -delete
```

**Vercel + Supabase版**:
- Supabaseの自動バックアップ機能を利用（Proプラン推奨）

#### ロールバック手順
```bash
# バックアップから復元
cat /backup/db-20260101.sql | docker exec -i postgres psql -U postgres reserve_db
```

### 6. アップデート戦略

#### メジャーアップデート（有償）
- 年1回程度の大型機能追加
- Next.jsメジャーバージョンアップ対応
- **料金**: 50,000〜100,000円

#### セキュリティパッチ（保守契約に含む）
- 脆弱性対応
- 依存パッケージのアップデート

---

## 🔧 コード共通化戦略

### 現在のマルチテナント設計を活かす

#### 共通コードベース
```
reserve-system/
├── reserve-app/              # 共通アプリケーション
│   ├── src/
│   │   ├── app/             # 全機能共通
│   │   ├── components/      # UI共通
│   │   ├── lib/             # ロジック共通
│   │   └── __tests__/       # テスト共通
│   ├── prisma/
│   │   └── schema.prisma    # DB共通
│   └── package.json
├── config/                   # 環境別設定（分岐ポイント）
│   ├── saas.env.example     # SaaS版設定
│   └── standalone.env.example # 買い切り版設定
└── deployment/               # デプロイ設定（分岐ポイント）
    ├── saas/
    │   └── vercel.json      # SaaS版Vercel設定
    └── standalone/
        ├── docker-compose.yml # VPS版Docker設定
        └── nginx.conf         # Nginx設定
```

### 分岐が必要な箇所

#### 1. 環境変数

**SaaS版** (`.env.saas`):
```bash
# マルチテナント対応
MULTI_TENANT_MODE=true
TENANT_ROUTING=subdomain  # customer-a.yourdomain.com

# 共有インフラ
DATABASE_URL="postgresql://shared-db.supabase.co"
NEXT_PUBLIC_SUPABASE_URL="https://shared.supabase.co"

# 機能制限
ENABLE_CUSTOM_BRANDING=false
MAX_STAFF_COUNT=10
MAX_RESERVATION_PER_MONTH=500
```

**買い切り版** (`.env.standalone`):
```bash
# シングルテナント
MULTI_TENANT_MODE=false
TENANT_ID=single-customer  # 固定

# 専用インフラ
DATABASE_URL="postgresql://localhost:5432/reserve_db"
NEXT_PUBLIC_SUPABASE_URL="https://customer-specific.supabase.co"

# 機能制限なし
ENABLE_CUSTOM_BRANDING=true
MAX_STAFF_COUNT=999
MAX_RESERVATION_PER_MONTH=999999
```

#### 2. テナント分離ロジック

**共通コード** (`src/lib/tenant.ts`):
```typescript
export function getTenantId(request?: Request): string {
  const multiTenantMode = process.env.MULTI_TENANT_MODE === 'true';

  if (!multiTenantMode) {
    // 買い切り版: 固定テナントID
    return process.env.TENANT_ID || 'single-customer';
  }

  // SaaS版: サブドメインから取得
  if (!request) {
    throw new Error('Request required in multi-tenant mode');
  }

  const hostname = request.headers.get('host') || '';
  const subdomain = hostname.split('.')[0];

  // customer-a.yourdomain.com → customer-a
  return subdomain;
}
```

#### 3. 機能制限ロジック

**共通コード** (`src/lib/limits.ts`):
```typescript
export function checkStaffLimit(currentCount: number): boolean {
  const maxStaff = parseInt(process.env.MAX_STAFF_COUNT || '999');
  return currentCount < maxStaff;
}

export function checkReservationLimit(
  tenantId: string,
  month: string
): Promise<boolean> {
  const maxReservations = parseInt(
    process.env.MAX_RESERVATION_PER_MONTH || '999999'
  );

  // DBで当月の予約数をカウント
  const count = await prisma.restaurantReservation.count({
    where: {
      tenantId,
      createdAt: {
        gte: new Date(`${month}-01`),
        lt: new Date(`${month}-31`),
      },
    },
  });

  return count < maxReservations;
}
```

### ビルドパイプラインの分岐

#### SaaS版ビルド
```bash
# .github/workflows/deploy-saas.yml
name: Deploy SaaS
on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: cp config/saas.env.example .env
      - run: npm ci
      - run: npm run build
      - uses: vercel/actions@v3
```

#### 買い切り版パッケージング
```bash
# scripts/package-standalone.sh
#!/bin/bash

# ソースコードをコピー
cp -r reserve-app standalone-package/

# 買い切り版の設定をコピー
cp config/standalone.env.example standalone-package/.env.example
cp deployment/standalone/docker-compose.yml standalone-package/

# ドキュメントを追加
cp documents/runbook/開発開始ガイド.md standalone-package/SETUP.md

# ZIPで圧縮
zip -r reserve-system-standalone-v1.0.0.zip standalone-package/
```

---

## 🏗️ インフラ選定

### SaaS型インフラ比較

| 項目 | Vercel + Supabase（推奨） | VPS（自前管理） | AWS（エンタープライズ） |
|-----|------------------------|---------------|---------------------|
| **初期コスト** | 低い | 中 | 高い |
| **運用コスト/月** | 約10,000円（10顧客） | 約5,000円 | 約50,000円〜 |
| **スケーラビリティ** | 高い（自動） | 中（手動） | 高い（自動） |
| **保守負担** | 低い | 高い | 中 |
| **セキュリティ** | マネージド | 自己責任 | 高度な設定可能 |
| **推奨顧客数** | 1-500 | 1-50 | 100+ |

#### 推奨構成（SaaS型）

**フェーズ1（MVP〜50顧客）**:
- Vercel Pro: $20/月
- Supabase Pro: $25/月
- Resend: $10/月
- **合計: $55/月（約8,250円）**

**フェーズ2（51〜500顧客）**:
- Vercel Pro: $20/月
- Supabase Pro + Read Replica: $50/月
- Resend Pro: $50/月
- Redis Cloud: $10/月
- **合計: $130/月（約19,500円）**

**フェーズ3（501顧客以上）**:
- Vercel Enterprise: 要見積もり
- AWS RDS Multi-AZ: 約$300/月
- AWS ElastiCache: 約$50/月
- **合計: 約$400/月（約60,000円）**

### 買い切り型インフラ比較

| 項目 | ConoHa VPS | さくらVPS | AWS Lightsail | Vercel + Supabase |
|-----|-----------|----------|--------------|------------------|
| **月額料金** | 2,000円〜 | 1,738円〜 | $5〜（約750円） | 0〜6,750円 |
| **メモリ** | 2GB〜 | 2GB〜 | 2GB〜 | 制限なし |
| **ストレージ** | 100GB〜 | 100GB〜 | 80GB〜 | 500MB（Free）〜 |
| **セットアップ難易度** | 中 | 中 | 中 | 低 |
| **推奨用途** | 中規模店舗 | 小規模店舗 | 小規模店舗 | 小規模店舗 |

#### 推奨構成（買い切り型）

**小規模店舗（予約数100件/月以下）**:
- Vercel Hobby（無料） + Supabase Free（無料）
- **コスト: 0円/月**
- 制限: Vercel Hobbyは商用利用時$20/月

**中規模店舗（予約数500件/月以下）**:
- ConoHa VPS 2GB: 2,000円/月
- Docker Compose構成
- **コスト: 2,000円/月**

**大規模店舗（予約数1000件/月以上）**:
- ConoHa VPS 4GB: 4,000円/月
- PostgreSQL専用サーバー
- **コスト: 4,000円/月**

---

## 🗄️ データベース設計

### SaaS型のDB戦略

#### 1. 現在の論理分離（tenant_id）- 推奨

**メリット**:
- ✅ 実装済み（追加開発不要）
- ✅ コスト効率が高い
- ✅ バックアップが一元管理できる

**デメリット**:
- ⚠️ テナント間のデータ漏洩リスク（RLSで対策）
- ⚠️ 1顧客の障害が全体に影響する可能性

**推奨規模**: 1-500顧客

#### 2. 物理分離（DB per Tenant）- 将来検討

```
顧客A → Supabase ProjectA (専用DB)
顧客B → Supabase ProjectB (専用DB)
顧客C → Supabase ProjectC (専用DB)
```

**メリット**:
- ✅ 完全な独立性
- ✅ 障害の影響範囲が限定的
- ✅ 顧客ごとのパフォーマンスチューニング可能

**デメリット**:
- ❌ コストが高い（$25/月 × 顧客数）
- ❌ 管理が複雑
- ❌ 一斉アップデートが困難

**推奨規模**: エンタープライズ顧客向け

#### 3. ハイブリッド（推奨: フェーズ2以降）

```
小規模顧客（1-50） → 共有DB（tenant_id分離）
大規模顧客（51+）  → 専用DB（物理分離）
```

### バックアップ戦略

#### SaaS型（論理分離）

**自動バックアップ**:
- Supabase Pro: 日次自動バックアップ + PITR（7日間）

**テナント別バックアップスクリプト**:
```bash
#!/bin/bash
# テナントごとにバックアップ取得（週次実行）

TENANTS=("customer-a" "customer-b" "customer-c")

for tenant in "${TENANTS[@]}"; do
  pg_dump --host=$DB_HOST \
          --username=$DB_USER \
          --dbname=$DB_NAME \
          --where="tenant_id = '$tenant'" \
          > backup-${tenant}-$(date +%Y%m%d).sql
done

# S3にアップロード
aws s3 sync ./backups s3://my-backups/
```

#### 買い切り型（VPS）

**自動バックアップ設定**:
```yaml
# docker-compose.yml
version: '3.8'
services:
  postgres:
    image: postgres:16
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./backups:/backups
    environment:
      POSTGRES_DB: reserve_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${DB_PASSWORD}

  # バックアップ用コンテナ
  backup:
    image: prodrigestivill/postgres-backup-local
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_DB: reserve_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      SCHEDULE: "@daily"  # 毎日バックアップ
      BACKUP_KEEP_DAYS: 14
    volumes:
      - ./backups:/backups
```

### パーティショニング・シャーディング

#### パーティショニング（テーブル分割）

**実装時期**: 顧客数100以上、またはテーブルサイズ10GB以上

```sql
-- 予約テーブルを日付でパーティション分割
CREATE TABLE restaurant_reservations (
  id UUID PRIMARY KEY,
  tenant_id VARCHAR NOT NULL,
  reserved_date DATE NOT NULL,
  ...
) PARTITION BY RANGE (reserved_date);

-- 月ごとにパーティション作成
CREATE TABLE reservations_2026_01 PARTITION OF restaurant_reservations
  FOR VALUES FROM ('2026-01-01') TO ('2026-02-01');

CREATE TABLE reservations_2026_02 PARTITION OF restaurant_reservations
  FOR VALUES FROM ('2026-02-01') TO ('2026-03-01');
```

**メリット**:
- クエリパフォーマンス向上
- 古いデータの削除が高速
- バックアップの効率化

#### シャーディング（データベース分割）

**実装時期**: 顧客数500以上

```
Shard 1 (DB1): tenant_id が 'a-m' で始まる顧客
Shard 2 (DB2): tenant_id が 'n-z' で始まる顧客
```

**ルーティングロジック**:
```typescript
function getShardConnection(tenantId: string): PrismaClient {
  const firstChar = tenantId[0].toLowerCase();

  if (firstChar >= 'a' && firstChar <= 'm') {
    return prismaClientShard1;
  } else {
    return prismaClientShard2;
  }
}
```

---

## 💡 推奨アプローチ

### フェーズ別戦略

#### フェーズ1: MVP〜初期顧客獲得（0-6ヶ月）

**販売方針**:
- ✅ SaaS型のみ提供（月額5,000円〜）
- ✅ 初期顧客5-10社を目標

**理由**:
- 継続収益モデルの確立
- フィードバック収集と改善サイクル
- 運用ノウハウの蓄積

**インフラ**:
- Vercel Pro + Supabase Pro
- 既存のマルチテナント設計を活用
- **コスト**: 約10,000円/月

#### フェーズ2: 成長期（6ヶ月〜1年）

**販売方針**:
- ✅ SaaS型（主力商品）
- ✅ 買い切り型（カスタマイズ需要に対応）

**SaaS型**:
- 目標顧客数: 30-50社
- プラン多様化（ライト/スタンダード/プレミアム）
- 月額収益: 30万円〜50万円

**買い切り型**:
- 中〜大規模店舗向け
- 価格: 30万円〜50万円
- 年間2-3件の受注を目標

**インフラ**:
- SaaS: Vercel Pro + Supabase Pro + Read Replica
- 買い切り: VPS構成のテンプレート化

#### フェーズ3: スケーリング期（1年〜）

**販売方針**:
- ✅ SaaS型（メイン）: エンタープライズプラン追加
- ✅ 買い切り型: パートナー経由の拡販

**SaaS型**:
- 目標顧客数: 100社以上
- エンタープライズ向け: 専用DB、カスタムドメイン、SLA保証
- 月額収益: 100万円〜

**買い切り型**:
- SI企業とのパートナー契約
- ホワイトラベル提供

**インフラ**:
- ハイブリッド構成（共有DB + 専用DB）
- シャーディング検討

### コード管理戦略

#### リポジトリ構成

```
reserve-system/               # メインリポジトリ
├── reserve-app/             # 共通コードベース
├── config/
│   ├── saas/               # SaaS版設定
│   └── standalone/         # 買い切り版設定
└── deployment/
    ├── saas/               # SaaS版デプロイ設定
    └── standalone/         # 買い切り版デプロイ設定
```

#### ブランチ戦略

```
main                        # 共通機能開発
├── release/saas-v1.0      # SaaS版リリース
└── release/standalone-v1.0 # 買い切り版リリース
```

### 開発優先順位

#### 最優先（今すぐ実装）
1. ✅ 既存機能の安定化（テストカバレッジ80%以上）
2. ✅ Supabase RLSの実装（セキュリティ強化）
3. ✅ プラン制限機能（スタッフ数、予約数）
4. ✅ デモ環境の構築（営業用）

#### 高優先（1-3ヶ月以内）
5. ✅ サブドメインルーティング（SaaS版）
6. ✅ 買い切り版のVPS構成テンプレート
7. ✅ 管理者ダッシュボード（顧客管理）
8. ✅ バックアップ自動化

#### 中優先（3-6ヶ月以内）
9. ✅ カスタムドメイン対応（エンタープライズプラン）
10. ✅ LINE連携（追加機能）
11. ✅ 決済機能（Stripe統合）
12. ✅ ホワイトラベル機能

---

## 📊 意思決定マトリックス

### SaaS型 vs 買い切り型 選択フローチャート

```
顧客の質問: 「初期コストを抑えたい？」
  YES → SaaS型を推奨
  NO  → 次へ

顧客の質問: 「ITリソースがある？」
  NO  → SaaS型を推奨
  YES → 次へ

顧客の質問: 「月額コストを避けたい？」
  YES → 買い切り型を推奨
  NO  → SaaS型を推奨

顧客の質問: 「カスタマイズが必要？」
  YES → 買い切り型（カスタマイズ版）を推奨
  NO  → SaaS型を推奨
```

### 収益シミュレーション（1年目）

#### SaaS型のみの場合
```
初月:  5顧客 × 5,000円 = 25,000円
3ヶ月: 15顧客 × 平均7,000円 = 105,000円
6ヶ月: 30顧客 × 平均8,000円 = 240,000円
12ヶ月: 50顧客 × 平均9,000円 = 450,000円

年間累計収益: 約250万円
インフラコスト: 約24万円（月2万円 × 12ヶ月）
粗利益: 約226万円
```

#### SaaS型 + 買い切り型の場合
```
SaaS: 30顧客 × 平均7,000円 × 12ヶ月 = 252万円
買い切り: 3件 × 40万円 = 120万円

年間累計収益: 約372万円
インフラコスト: 約18万円（SaaS分のみ）
開発コスト: 約60万円（買い切りカスタマイズ）
粗利益: 約294万円
```

### 推奨戦略

**結論**: **SaaS型を主力とし、買い切り型を補完的に提供**

**理由**:
1. ✅ 継続収益モデルで安定した売上
2. ✅ 顧客との長期的な関係構築
3. ✅ フィードバックを活かした改善サイクル
4. ✅ インフラコストの効率化
5. ✅ 買い切り型は大口顧客やカスタマイズ需要に対応

---

## 📋 次のアクション

### 即実装すべきタスク

1. **RLSポリシーの実装** (優先度: 最高)
   - Supabaseでテナント分離の強化
   - セキュリティ監査

2. **プラン制限機能** (優先度: 高)
   - スタッフ数上限チェック
   - 予約数上限チェック
   - プラン別機能の有効化/無効化

3. **デモ環境** (優先度: 高)
   - demo-restaurant テナントの充実
   - サンプルデータの投入
   - 営業資料の作成

4. **サブドメインルーティング** (優先度: 高)
   - ミドルウェアでのテナントID抽出
   - 動的ルーティング実装

5. **買い切り版パッケージング** (優先度: 中)
   - Docker Compose構成
   - セットアップスクリプト
   - 納品用ドキュメント

---

**このドキュメントを基に、具体的な実装計画を立ててください。**
