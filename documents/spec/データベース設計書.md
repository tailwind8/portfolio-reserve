# データベース設計書

**最終更新**: 2026-01-04
**データベース**: Supabase PostgreSQL
**ORM**: Prisma 7

---

## 📊 ER図

```
┌─────────────────────┐
│ booking_users       │
├─────────────────────┤        ┌─────────────────────┐
│ id (PK)             │        │ feature_flags       │
│ tenant_id           │        ├─────────────────────┤
│ auth_id (UK)        │        │ id (PK)             │
│ email               │        │ tenant_id (UK)      │
│ name                │        │ enable_*（10種類）   │
│ phone               │        │ created_at          │
│ memo                │        │ updated_at          │
│ role                │        └─────────────────────┘
│ created_at          │
│ updated_at          │        ┌─────────────────────┐
└─────────────────────┘        │ security_logs       │
         │                     ├─────────────────────┤
         │ 1                   │ id (PK)             │
         │                     │ tenant_id           │
         │ *                   │ event_type          │
         ▼                     │ user_id             │
┌─────────────────────┐        │ ip_address          │
│ booking_            │        │ user_agent          │
│ reservations        │        │ metadata            │
├─────────────────────┤        │ created_at          │
│ id (PK)             │        └─────────────────────┘
│ tenant_id           │
│ user_id (FK)        │  *     ┌─────────────────────┐
│ staff_id (FK)       │────────│ booking_menus       │
│ menu_id (FK)        │        ├─────────────────────┤
│ reserved_date       │        │ id (PK)             │
│ reserved_time       │        │ tenant_id           │
│ status              │        │ name                │
│ notes               │        │ description         │
│ reminder_sent       │        │ price               │
│ created_at          │        │ duration            │
│ updated_at          │        │ is_active           │
└─────────────────────┘        │ category            │
         │                     │ created_at          │
         │ *                   │ updated_at          │
         │                     └─────────────────────┘
         │ 1
         ▼
┌─────────────────────┐        ┌─────────────────────┐
│ booking_staff       │        │ booking_settings    │
├─────────────────────┤        ├─────────────────────┤
│ id (PK)             │        │ id (PK)             │
│ tenant_id           │        │ tenant_id (UK)      │
│ name                │        │ store_name          │
│ email               │        │ store_email         │
│ phone               │        │ store_phone         │
│ role                │        │ open_time           │
│ is_active           │        │ close_time          │
│ created_at          │        │ closed_days         │
│ updated_at          │        │ slot_duration       │
└─────────────────────┘        │ is_public           │
         │                     │ min/max_advance_days│
         │ 1                   │ cancel_deadline_hrs │
         │                     │ created_at          │
         │ *                   │ updated_at          │
         ▼                     └─────────────────────┘
┌─────────────────────┐
│ booking_staff_      │        ┌─────────────────────┐
│ shifts              │        │ booking_blocked_    │
├─────────────────────┤        │ time_slots          │
│ id (PK)             │        ├─────────────────────┤
│ tenant_id           │        │ id (PK)             │
│ staff_id (FK)       │        │ tenant_id           │
│ day_of_week         │        │ start_date_time     │
│ start_time          │        │ end_date_time       │
│ end_time            │        │ reason              │
│ is_active           │        │ description         │
│ created_at          │        │ created_by          │
│ updated_at          │        │ created_at          │
└─────────────────────┘        │ updated_at          │
                               └─────────────────────┘
┌─────────────────────┐
│ booking_staff_      │
│ vacations           │
├─────────────────────┤
│ id (PK)             │
│ tenant_id           │
│ staff_id (FK)       │
│ start_date          │
│ end_date            │
│ reason              │
│ created_at          │
│ updated_at          │
└─────────────────────┘
```

---

## 📋 テーブル定義

### 1. booking_users（ユーザー）

**目的**: 予約システムを利用する顧客の情報を管理

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | UUID | NO | uuid() | プライマリキー |
| `tenant_id` | VARCHAR | NO | 'demo-booking' | テナントID（マルチテナント対応） |
| `auth_id` | UUID | YES | NULL | Supabase Auth UUID |
| `email` | VARCHAR | NO | - | メールアドレス |
| `name` | VARCHAR | YES | NULL | 氏名 |
| `phone` | VARCHAR | YES | NULL | 電話番号 |
| `memo` | VARCHAR | YES | '' | 顧客メモ（500文字以内） |
| `role` | ENUM | NO | CUSTOMER | ユーザーロール（CUSTOMER/ADMIN/SUPER_ADMIN） |
| `created_at` | TIMESTAMP | NO | now() | 作成日時 |
| `updated_at` | TIMESTAMP | NO | now() | 更新日時 |

**制約**:
- PRIMARY KEY: `id`
- UNIQUE: `(tenant_id, email)` - 同一テナント内でメールアドレスは一意
- UNIQUE: `auth_id` - Supabase Auth IDは一意
- INDEX: `tenant_id` - テナント別クエリの高速化
- INDEX: `role` - ロール検索用インデックス

**ENUM: UserRole**
- `CUSTOMER` - 一般ユーザー（顧客）
- `ADMIN` - 店舗管理者
- `SUPER_ADMIN` - スーパー管理者（開発者）

**リレーション**:
- `booking_reservations.user_id` → `booking_users.id` (1:N)

---

### 2. booking_staff（スタッフ）

**目的**: 店舗のスタッフ情報を管理

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | UUID | NO | uuid() | プライマリキー |
| `tenant_id` | VARCHAR | NO | 'demo-booking' | テナントID |
| `name` | VARCHAR | NO | - | スタッフ名 |
| `role` | VARCHAR | YES | NULL | 役職（例: スタイリスト） |
| `is_active` | BOOLEAN | NO | true | 有効/無効フラグ |
| `created_at` | TIMESTAMP | NO | now() | 作成日時 |
| `updated_at` | TIMESTAMP | NO | now() | 更新日時 |

**制約**:
- PRIMARY KEY: `id`
- INDEX: `tenant_id`

**リレーション**:
- `booking_reservations.staff_id` → `booking_staff.id` (1:N)

---

### 3. booking_menus（メニュー）

**目的**: サービスメニューの情報を管理

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | UUID | NO | uuid() | プライマリキー |
| `tenant_id` | VARCHAR | NO | 'demo-booking' | テナントID |
| `name` | VARCHAR | NO | - | メニュー名 |
| `description` | TEXT | YES | NULL | 説明 |
| `price` | INTEGER | NO | - | 価格（円） |
| `duration` | INTEGER | NO | - | 所要時間（分） |
| `is_active` | BOOLEAN | NO | true | 有効/無効フラグ |
| `category` | VARCHAR | YES | NULL | カテゴリ（例: カット、カラー） |
| `created_at` | TIMESTAMP | NO | now() | 作成日時 |
| `updated_at` | TIMESTAMP | NO | now() | 更新日時 |

**制約**:
- PRIMARY KEY: `id`
- INDEX: `tenant_id`

**リレーション**:
- `booking_reservations.menu_id` → `booking_menus.id` (1:N)

---

### 4. booking_reservations（予約）

**目的**: 予約情報を管理（システムのコアテーブル）

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | UUID | NO | uuid() | プライマリキー |
| `tenant_id` | VARCHAR | NO | 'demo-booking' | テナントID |
| `user_id` | UUID | NO | - | ユーザーID（FK） |
| `staff_id` | UUID | NO | - | スタッフID（FK） |
| `menu_id` | UUID | NO | - | メニューID（FK） |
| `reserved_date` | DATE | NO | - | 予約日 |
| `reserved_time` | VARCHAR | NO | - | 予約時間（例: "14:00"） |
| `status` | ENUM | NO | PENDING | 予約ステータス |
| `notes` | TEXT | YES | NULL | 備考・要望 |
| `created_at` | TIMESTAMP | NO | now() | 作成日時 |
| `updated_at` | TIMESTAMP | NO | now() | 更新日時 |

**制約**:
- PRIMARY KEY: `id`
- FOREIGN KEY: `user_id` → `booking_users.id` (CASCADE DELETE)
- FOREIGN KEY: `staff_id` → `booking_staff.id`
- FOREIGN KEY: `menu_id` → `booking_menus.id`
- INDEX: `tenant_id`
- INDEX: `user_id`
- INDEX: `reserved_date`

**ENUM: ReservationStatus**
- `PENDING` - 予約確定待ち
- `CONFIRMED` - 確定
- `CANCELLED` - キャンセル
- `COMPLETED` - 完了（来店済み）
- `NO_SHOW` - 無断キャンセル

**リレーション**:
- `user_id` → `booking_users.id` (N:1)
- `staff_id` → `booking_staff.id` (N:1)
- `menu_id` → `booking_menus.id` (N:1)

---

### 5. booking_settings（店舗設定）

**目的**: 店舗の基本情報と営業設定を管理

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | UUID | NO | uuid() | プライマリキー |
| `tenant_id` | VARCHAR | NO | 'demo-booking' | テナントID |
| `store_name` | VARCHAR | NO | - | 店舗名 |
| `store_email` | VARCHAR | YES | NULL | 店舗メールアドレス |
| `store_phone` | VARCHAR | YES | NULL | 店舗電話番号 |
| `open_time` | VARCHAR | NO | '09:00' | 開店時刻 |
| `close_time` | VARCHAR | NO | '20:00' | 閉店時刻 |
| `closed_days` | VARCHAR[] | NO | [] | 定休日（例: ["Sunday"]） |
| `slot_duration` | INTEGER | NO | 30 | 予約枠の間隔（分） |
| `is_public` | BOOLEAN | NO | true | システム公開・非公開フラグ |
| `min_advance_booking_days` | INTEGER | NO | 0 | 何日後から予約可能 |
| `max_advance_booking_days` | INTEGER | NO | 90 | 何日後まで予約可能 |
| `cancellation_deadline_hours` | INTEGER | NO | 24 | 予約日の何時間前までキャンセル可能 |
| `created_at` | TIMESTAMP | NO | now() | 作成日時 |
| `updated_at` | TIMESTAMP | NO | now() | 更新日時 |

**制約**:
- PRIMARY KEY: `id`
- UNIQUE: `tenant_id` - 1テナント1設定

---

### 6. booking_staff_shifts（スタッフシフト）

**目的**: スタッフの曜日別出勤時間を管理

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | UUID | NO | uuid() | プライマリキー |
| `tenant_id` | VARCHAR | NO | 'demo-booking' | テナントID |
| `staff_id` | UUID | NO | - | スタッフID（FK） |
| `day_of_week` | ENUM | NO | - | 曜日 |
| `start_time` | VARCHAR | NO | - | 出勤時間（例: "09:00"） |
| `end_time` | VARCHAR | NO | - | 退勤時間（例: "18:00"） |
| `is_active` | BOOLEAN | NO | true | 有効フラグ |
| `created_at` | TIMESTAMP | NO | now() | 作成日時 |
| `updated_at` | TIMESTAMP | NO | now() | 更新日時 |

**制約**:
- PRIMARY KEY: `id`
- UNIQUE: `(tenant_id, staff_id, day_of_week)` - 同一スタッフの同一曜日は一意
- FOREIGN KEY: `staff_id` → `booking_staff.id` (CASCADE DELETE)
- INDEX: `tenant_id`
- INDEX: `staff_id`

**ENUM: DayOfWeek**
- `MONDAY`, `TUESDAY`, `WEDNESDAY`, `THURSDAY`, `FRIDAY`, `SATURDAY`, `SUNDAY`

**リレーション**:
- `staff_id` → `booking_staff.id` (N:1)

---

### 7. booking_staff_vacations（スタッフ休暇）

**目的**: スタッフの休暇・特別休業日を管理

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | UUID | NO | uuid() | プライマリキー |
| `tenant_id` | VARCHAR | NO | 'demo-booking' | テナントID |
| `staff_id` | UUID | NO | - | スタッフID（FK） |
| `start_date` | DATE | NO | - | 休暇開始日 |
| `end_date` | DATE | NO | - | 休暇終了日 |
| `reason` | VARCHAR | YES | NULL | 理由（任意） |
| `created_at` | TIMESTAMP | NO | now() | 作成日時 |
| `updated_at` | TIMESTAMP | NO | now() | 更新日時 |

**制約**:
- PRIMARY KEY: `id`
- FOREIGN KEY: `staff_id` → `booking_staff.id` (CASCADE DELETE)
- INDEX: `tenant_id`
- INDEX: `staff_id`
- INDEX: `(start_date, end_date)` - 期間検索用

**リレーション**:
- `staff_id` → `booking_staff.id` (N:1)

---

### 8. feature_flags（機能フラグ）

**目的**: テナント別のオプション機能ON/OFF管理（SUPER_ADMIN専用）

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | UUID | NO | uuid() | プライマリキー |
| `tenant_id` | VARCHAR | NO | 'demo-booking' | テナントID |
| `enable_staff_selection` | BOOLEAN | NO | false | スタッフ指名機能 |
| `enable_staff_shift_management` | BOOLEAN | NO | false | スタッフシフト管理 |
| `enable_customer_management` | BOOLEAN | NO | true | 顧客管理・メモ機能 |
| `enable_reservation_update` | BOOLEAN | NO | false | 予約変更機能 |
| `enable_reminder_email` | BOOLEAN | NO | true | リマインダーメール |
| `enable_manual_reservation` | BOOLEAN | NO | true | 予約手動追加 |
| `enable_analytics_report` | BOOLEAN | NO | true | 分析レポート |
| `enable_repeat_rate_analysis` | BOOLEAN | NO | false | リピート率分析 |
| `enable_coupon_feature` | BOOLEAN | NO | false | クーポン機能 |
| `enable_line_notification` | BOOLEAN | NO | false | LINE通知連携 |
| `created_at` | TIMESTAMP | NO | now() | 作成日時 |
| `updated_at` | TIMESTAMP | NO | now() | 更新日時 |

**制約**:
- PRIMARY KEY: `id`
- UNIQUE: `tenant_id` - 1テナント1設定

---

### 9. booking_blocked_time_slots（予約ブロック時間帯）

**目的**: ホットペッパー予約や電話予約など、システム外で受け付けた予約に対応し、該当時間帯をオンライン予約から除外

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | UUID | NO | uuid() | プライマリキー |
| `tenant_id` | VARCHAR | NO | 'demo-booking' | テナントID |
| `start_date_time` | TIMESTAMP | NO | - | ブロック開始日時 |
| `end_date_time` | TIMESTAMP | NO | - | ブロック終了日時 |
| `reason` | VARCHAR | YES | NULL | 理由（例: "ホットペッパー予約"） |
| `description` | VARCHAR | YES | NULL | 詳細説明（任意） |
| `created_by` | UUID | YES | NULL | 作成者（管理者ID） |
| `created_at` | TIMESTAMP | NO | now() | 作成日時 |
| `updated_at` | TIMESTAMP | NO | now() | 更新日時 |

**制約**:
- PRIMARY KEY: `id`
- INDEX: `tenant_id`
- INDEX: `(start_date_time, end_date_time)` - 時間範囲検索用
- INDEX: `(tenant_id, start_date_time, end_date_time)` - 空き時間確認時の複合インデックス

---

### 10. security_logs（セキュリティ監査ログ）

**目的**: セキュリティイベントの記録（ログイン試行、不正アクセス検知など）

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | UUID | NO | uuid() | プライマリキー |
| `tenant_id` | VARCHAR | NO | 'demo-booking' | テナントID |
| `event_type` | VARCHAR | NO | - | イベント種別 |
| `user_id` | UUID | YES | NULL | ログインユーザーID |
| `ip_address` | VARCHAR | YES | NULL | リクエスト元IPアドレス |
| `user_agent` | VARCHAR | YES | NULL | User-Agent文字列 |
| `metadata` | JSON | YES | NULL | その他の情報（email、reason、pathなど） |
| `created_at` | TIMESTAMP | NO | now() | 作成日時 |

**イベント種別（event_type）**:
- `LOGIN_SUCCESS` - ログイン成功
- `LOGIN_FAILED` - ログイン失敗
- `USER_REGISTER` - ユーザー登録
- `LOGOUT` - ログアウト
- `UNAUTHORIZED_ACCESS` - 不正アクセス試行
- `RATE_LIMIT_EXCEEDED` - レート制限超過
- `CSRF_VALIDATION_FAILED` - CSRF検証失敗

**制約**:
- PRIMARY KEY: `id`
- INDEX: `tenant_id`
- INDEX: `event_type`
- INDEX: `user_id`
- INDEX: `created_at`
- INDEX: `(tenant_id, event_type, created_at)` - イベント種別・日時での検索用

---

## 🔐 マルチテナント設計

### 概要
複数のポートフォリオプロジェクトで同一データベースを共有するため、テナント分離を実装

### 実装方針

#### 1. テーブル名プレフィックス
- `booking_*` - 予約システム
- `hotel_*` - ホテル予約システム（将来）
- `clinic_*` - クリニック予約システム（将来）

#### 2. tenant_id による論理分離
- 全テーブルに `tenant_id` カラムを配置
- デフォルト値: `'demo-booking'`
- クエリ時に必ず `tenant_id` でフィルタ

#### 3. Prismaでの実装例

```typescript
// テナントIDを環境変数から取得
const TENANT_ID = process.env.NEXT_PUBLIC_TENANT_ID || 'demo-booking';

// クエリ時に必ずテナントIDでフィルタ
const users = await prisma.restaurantUser.findMany({
  where: {
    tenantId: TENANT_ID,
  },
});
```

#### 4. Row Level Security (RLS) の活用（将来）

Supabase RLSを使用したセキュリティ強化:

```sql
-- ユーザーは自身のテナントのデータのみ閲覧可能
CREATE POLICY tenant_isolation ON booking_users
  USING (tenant_id = current_setting('app.current_tenant'));
```

---

## 📈 インデックス戦略

### 目的別インデックス

| テーブル | カラム | 目的 |
|---------|-------|------|
| `booking_users` | `tenant_id` | テナント別フィルタ |
| `booking_users` | `auth_id` | Supabase Auth連携 |
| `booking_users` | `(tenant_id, email)` | ログイン高速化 |
| `booking_staff` | `tenant_id` | テナント別フィルタ |
| `booking_menus` | `tenant_id` | テナント別フィルタ |
| `booking_reservations` | `tenant_id` | テナント別フィルタ |
| `booking_reservations` | `user_id` | ユーザー予約一覧 |
| `booking_reservations` | `reserved_date` | 日付検索 |

### 複合インデックス検討

```sql
-- 予約検索の高速化
CREATE INDEX idx_reservations_tenant_date
  ON booking_reservations(tenant_id, reserved_date);

-- スタッフ別予約検索
CREATE INDEX idx_reservations_staff_date
  ON booking_reservations(staff_id, reserved_date);
```

---

## 🔒 データ整合性制約

### 外部キー制約

| 親テーブル | 子テーブル | カスケード |
|----------|----------|----------|
| `booking_users` → `booking_reservations` | user_id | CASCADE DELETE |
| `booking_staff` → `booking_reservations` | staff_id | RESTRICT |
| `booking_menus` → `booking_reservations` | menu_id | RESTRICT |

**CASCADE DELETE理由**:
- ユーザー削除時、関連する予約も削除（GDPR対応）

**RESTRICT理由**:
- スタッフ/メニュー削除時、関連予約がある場合は削除不可（データ保護）

---

## 🧪 サンプルデータ

### 初期データ投入（Seed）

```typescript
// prisma/seed.ts
import { PrismaClient } from '@prisma/client';

const prisma = new PrismaClient();

async function main() {
  const TENANT_ID = 'demo-booking';

  // 店舗設定
  await prisma.restaurantSettings.create({
    data: {
      tenantId: TENANT_ID,
      storeName: 'サンプル美容室',
      storeEmail: 'info@sample-salon.com',
      storePhone: '03-1234-5678',
      openTime: '10:00',
      closeTime: '20:00',
      closedDays: ['Monday'],
      slotDuration: 30,
    },
  });

  // スタッフ
  const staff = await prisma.restaurantStaff.createMany({
    data: [
      { tenantId: TENANT_ID, name: '田中太郎', role: 'スタイリスト' },
      { tenantId: TENANT_ID, name: '佐藤花子', role: 'シニアスタイリスト' },
    ],
  });

  // メニュー
  const menus = await prisma.restaurantMenu.createMany({
    data: [
      { tenantId: TENANT_ID, name: 'カット', price: 5000, duration: 60, category: 'カット' },
      { tenantId: TENANT_ID, name: 'カラー', price: 8000, duration: 90, category: 'カラー' },
      { tenantId: TENANT_ID, name: 'パーマ', price: 10000, duration: 120, category: 'パーマ' },
    ],
  });

  console.log('Seed data inserted successfully');
}

main()
  .catch((e) => {
    console.error(e);
    process.exit(1);
  })
  .finally(async () => {
    await prisma.$disconnect();
  });
```

---

## 📊 想定クエリパターン

### 1. 予約一覧取得（管理者）

```typescript
// 特定日の予約一覧
const reservations = await prisma.restaurantReservation.findMany({
  where: {
    tenantId: TENANT_ID,
    reservedDate: new Date('2025-01-20'),
  },
  include: {
    user: true,
    staff: true,
    menu: true,
  },
  orderBy: {
    reservedTime: 'asc',
  },
});
```

### 2. ユーザーの予約履歴取得

```typescript
const userReservations = await prisma.restaurantReservation.findMany({
  where: {
    tenantId: TENANT_ID,
    userId: 'user-uuid',
  },
  include: {
    staff: true,
    menu: true,
  },
  orderBy: {
    reservedDate: 'desc',
  },
});
```

### 3. 空き時間検索

```typescript
// 特定日時のスタッフの予約状況チェック
const conflictingReservations = await prisma.restaurantReservation.findFirst({
  where: {
    tenantId: TENANT_ID,
    staffId: 'staff-uuid',
    reservedDate: new Date('2025-01-20'),
    reservedTime: '14:00',
    status: {
      not: 'CANCELLED',
    },
  },
});

// 空いている場合: conflictingReservations === null
```

---

## 🔄 マイグレーション戦略

### 開発環境

```bash
# スキーマ変更後
npx prisma migrate dev --name add_new_field

# Prisma Client再生成
npx prisma generate
```

### 本番環境

```bash
# 本番デプロイ前に検証環境でマイグレーション確認
npx prisma migrate deploy
```

### ロールバック戦略

```bash
# 特定のマイグレーションに戻す
npx prisma migrate resolve --rolled-back <migration-name>
```

---

## 📚 関連ドキュメント

- `reserve-app/prisma/schema.prisma` - Prismaスキーマ定義
- `api/API設計書.md` - API仕様
- `architecture/システムアーキテクチャ.md` - システム構成
- `architecture/マルチテナント設計.md` - テナント分離設計

---

## 🚀 今後の拡張予定

### Phase 2
- カルテテーブル追加（施術履歴、写真）
- クーポンテーブル追加

### Phase 3
- 分析用集計テーブル
- リマインダー送信履歴テーブル

---

**このデータベース設計は、スケーラビリティと保守性を重視しています。**
