# 規約優先順位ガイド

**最終更新**: 2026-01-06
**対象**: AIエージェント、開発者

このドキュメントは、プロジェクト内の規約を優先順位順に整理したものです。

---

## 🚨 優先度: CRITICAL（必須・CIで失敗）

以下の規約は**CIで自動チェック**され、違反するとPRがマージできません。

### 1. シークレット漏洩防止（CI失敗）
- ❌ APIキー、JWTトークン、データベースURLのハードコード禁止
- ✅ 環境変数経由で管理

```typescript
// ❌ 禁止
const apiKey = "sk_live_xxxxxxxxxx";
const dbUrl = "postgresql://user:pass@host/db";

// ✅ 推奨
const apiKey = process.env.API_KEY;
const dbUrl = process.env.DATABASE_URL;
```

**チェック**: `.github/workflows/code-standards.yml` のsecrets-check（**CI失敗**）

### 2. TypeScript型安全性
- ❌ `any`型の使用禁止
- ✅ `unknown`型 + 型ガードを使用

```typescript
// ❌ 禁止
const data: any = response;

// ✅ 推奨
const data: unknown = response;
if (isValidResponse(data)) { ... }
```

**チェック**: `npm run lint`, `npm run build:ci`, code-quality-check

### 3. ESLintルール遵守
- エラー0件であること

**チェック**: `npm run lint`

### 4. テスト通過
- 単体テスト: `npm test`
- E2Eスモーク: `npm run test:e2e:smoke`

---

## ⚠️ 優先度: HIGH（強く推奨）

以下の規約は**CIで警告**されます。既存コード修正後、必須化予定。

### 5. npm auditセキュリティチェック

**対象**: 依存パッケージのセキュリティ脆弱性

```bash
# high以上の脆弱性をチェック
npm audit --audit-level=high
```

**チェック**: `.github/workflows/code-standards.yml` のsecurity-audit

### 6. 危険なパターンの禁止

**対象**: XSS、SQLインジェクション、コードインジェクションのリスク

```typescript
// ❌ 禁止
<div dangerouslySetInnerHTML={{ __html: userInput }} />  // XSSリスク
prisma.$queryRawUnsafe(userInput);  // SQLインジェクションリスク
eval(userCode);  // コードインジェクションリスク

// ✅ 安全な代替
// - DOMPurifyでサニタイズ
// - prisma.$queryRaw`` (テンプレートリテラル使用)
// - evalは使用しない
```

**チェック**: `.github/workflows/code-standards.yml` のdangerous-patterns-check

### 7. E2Eテストでdata-testidセレクタを使用

**現状の問題**: `getByText`, `:has-text`が多用されている

```typescript
// ❌ 非推奨（テキスト変更で壊れる）
await page.getByText('予約を確定する').click();
await page.locator('button:has-text("予約")').click();

// ✅ 推奨（安定したセレクタ）
await page.locator('[data-testid="submit-booking"]').click();
```

**チェック**: `.github/workflows/code-standards.yml` のtest-selector-check

### 8. APIルートでZodバリデーション

**対象**: POST/PUT/PATCHメソッドを持つAPIルート

```typescript
// ✅ 必須パターン
import { z } from 'zod';

const schema = z.object({
  name: z.string().min(1),
  email: z.string().email(),
});

export async function POST(request: Request) {
  const body = await request.json();
  const validated = schema.safeParse(body);

  if (!validated.success) {
    return Response.json({ error: validated.error }, { status: 400 });
  }
  // ...
}
```

**チェック**: `.github/workflows/code-standards.yml` のapi-validation-check

### 9. tenant_idフィルタリング（マルチテナント）

**対象**: Prismaクエリを含む全APIルート

```typescript
// ❌ 危険（他テナントのデータが見える）
const reservations = await prisma.restaurantReservation.findMany();

// ✅ 安全
const reservations = await prisma.restaurantReservation.findMany({
  where: {
    tenant_id: TENANT_ID,
    // ...
  },
});
```

**チェック**: `.github/workflows/code-standards.yml` のtenant-filter-check

### 10. console.log/debug禁止

**対象**: 本番コード（テストファイル除く）

```typescript
// ❌ 禁止
console.log("debug:", data);
console.debug("state:", state);

// ✅ 推奨
// - デバッグログは削除
// - 必要な場合はロガー（Sentry等）を使用
```

**チェック**: `.github/workflows/code-standards.yml` のcode-quality-check

### 11. 日本語運用ルール

**対象**: コミットメッセージ、PRタイトル

```bash
# ✅ 日本語で記述
git commit -m "feat: ユーザー登録機能を実装"

# ❌ 英語のみは非推奨
git commit -m "feat: Add user registration"
```

**チェック**: `.github/workflows/code-standards.yml` のjapanese-check

### 12. テストカバレッジしきい値

**目標値**:
- Lines: 70%以上
- Branches: 60%以上
- Functions: 70%以上

**チェック**: `.github/workflows/code-standards.yml` のcoverage-threshold

### 13. TODO/FIXME管理

**対象**: 本番コード（テストファイル除く）

```typescript
// ⚠️ 検出対象
// TODO: 後で実装する
// FIXME: バグがある
// HACK: 一時的な対応
// XXX: 注意が必要
```

**チェック**: `.github/workflows/code-standards.yml` のtodo-check

### 14. コード複雑度

**しきい値**:
- サイクロマティック複雑度: 15以下
- ネスト深度: 4以下
- パラメータ数: 5以下
- 関数行数: 60行以下

**チェック**: `.github/workflows/code-standards.yml` のcomplexity-check

### 15. エラーメッセージ漏洩防止

**対象**: APIルートのエラーハンドリング

```typescript
// ❌ 危険（内部情報漏洩）
return Response.json({ error: error.message }, { status: 500 });

// ✅ 安全
console.error('Internal error:', error);
return Response.json({ error: 'An error occurred' }, { status: 500 });
```

**チェック**: `.github/workflows/code-standards.yml` のerror-leak-check

### 16. クライアント環境変数制限

**対象**: `'use client'`ファイル

```typescript
// ❌ 危険（秘密鍵がバンドルに含まれる）
'use client';
const key = process.env.SUPABASE_SERVICE_ROLE_KEY;

// ✅ 安全（公開用のみ）
'use client';
const key = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;
```

**チェック**: `.github/workflows/code-standards.yml` のclient-env-check

### 17. コマンドインジェクション防止

**対象**: child_process使用

```typescript
// ❌ 禁止
import { exec } from 'child_process';
exec(`rm -rf ${userInput}`);
```

**チェック**: `.github/workflows/code-standards.yml` のcommand-injection-check

### 18. セキュリティヘッダー設定

**必須ヘッダー**:
- X-Frame-Options
- X-Content-Type-Options
- Strict-Transport-Security
- Content-Security-Policy
- Referrer-Policy

**チェック**: `.github/workflows/code-standards.yml` のsecurity-headers-check

---

## 📋 優先度: MEDIUM（推奨）

以下の規約は**手動レビュー**で確認されます。

### 19. Page Objectパターン使用

**対象**: 新規E2Eテスト

```typescript
// ✅ Page Objectを作成
// src/__tests__/e2e/pages/BookingPage.ts
export class BookingPage {
  constructor(private page: Page) {}

  private selectors = {
    submitButton: '[data-testid="submit-booking"]',
  };

  async submit() {
    await this.page.click(this.selectors.submitButton);
  }
}

// テストで使用
const bookingPage = new BookingPage(page);
await bookingPage.submit();
```

### 20. Gherkinシナリオ先行（ATDD/BDD）

**対象**: 新機能開発

**開発フロー**:
1. Gherkinシナリオ作成（`features/`ディレクトリ）
2. E2Eテスト実装（Red）
3. 最小実装（Green）
4. リファクタリング

### 21. コンポーネントにdata-testid追加

**対象**: button, input, select等のインタラクティブ要素

```tsx
<button
  data-testid="submit-booking"
  onClick={handleSubmit}
>
  予約を確定する
</button>
```

### 22. アクセシビリティ対応

- セマンティックHTML使用
- aria-label属性追加
- キーボード操作対応

---

## 📝 優先度: LOW（任意）

以下は品質向上のための推奨事項です。

### 23. コードコメント（日本語）

複雑なビジネスロジックには日本語コメントを追加。

### 24. 単体テストカバレッジ80%以上

新規コードはカバレッジ80%を目標に。

---

## 📊 チェック方法まとめ

| 優先度 | チェック方法 | 失敗時 |
|--------|-------------|--------|
| CRITICAL | CI自動チェック | PRマージ不可 |
| HIGH | CI警告 | 警告表示（将来必須化） |
| MEDIUM | 手動レビュー | レビューで指摘 |
| LOW | 任意 | 推奨のみ |

### GitHub Actions ジョブ一覧（全17ジョブ）

| # | ジョブ名 | チェック内容 | 優先度 |
|---|---------|-------------|--------|
| 1 | `test-selector-check` | E2Eセレクタ規約 | HIGH（警告） |
| 2 | `api-validation-check` | Zodバリデーション | HIGH（警告） |
| 3 | `gherkin-check` | Gherkinシナリオ | HIGH（警告） |
| 4 | `testid-check` | data-testid属性 | HIGH（警告） |
| 5 | `tenant-filter-check` | tenant_idフィルタ | HIGH（警告） |
| 6 | `security-audit` | npm audit脆弱性 | HIGH（警告） |
| 7 | `dangerous-patterns-check` | XSS/SQLi/eval | HIGH（警告） |
| 8 | `secrets-check` | シークレット漏洩 | **CRITICAL（失敗）** |
| 9 | `code-quality-check` | any型/console.log | HIGH（警告） |
| 10 | `japanese-check` | 日本語運用 | HIGH（警告） |
| 11 | `coverage-threshold` | カバレッジしきい値 | HIGH（警告） |
| 12 | `todo-check` | TODO/FIXME検出 | HIGH（警告） |
| 13 | `complexity-check` | コード複雑度 | HIGH（警告） |
| 14 | `error-leak-check` | エラーメッセージ漏洩 | HIGH（警告） |
| 15 | `client-env-check` | クライアント環境変数 | HIGH（警告） |
| 16 | `command-injection-check` | コマンドインジェクション | HIGH（警告） |
| 17 | `security-headers-check` | セキュリティヘッダー | HIGH（警告） |

---

## 🔧 規約違反の修正方法

### E2Eテストセレクタ違反の修正

1. コンポーネントに`data-testid`を追加
2. Page Objectのセレクタを更新
3. `getByText`/`:has-text`を`[data-testid="..."]`に置換

**優先修正ファイル**:
- `src/__tests__/e2e/pages/MyPage.ts`（37件）
- `src/__tests__/e2e/pages/LoginPage.ts`（11件）
- `src/__tests__/e2e/booking-concurrency.spec.ts`（12件）

### Zodバリデーション追加

1. `src/lib/validations.ts`にスキーマを追加
2. APIルートでimportしてsafeParseを使用

---

## 📚 関連ドキュメント

| ドキュメント | 内容 |
|-------------|------|
| `.cursor/rules/開発プロセスルール.mdc` | テスト・実装の詳細ルール |
| `documents/コード品質チェックリスト.md` | PR前チェック項目 |
| `.claude/skills/typescript-security-checker/references/security-checklist.md` | セキュリティ規約 |
| `CLAUDE.md` | プロジェクト全体ガイド |

---

**このガイドに従うことで、CIチェックをパスし、高品質なコードを維持できます。**
