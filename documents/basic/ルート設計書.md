# ルート設計書 - 予約管理システム

**作成日**: 2026-01-01
**最終更新**: 2026-01-04
**プロジェクト**: 予約管理システム（美容室・歯科医院・整体院など多業種対応）
**バージョン**: 2.0.0
**フレームワーク**: Next.js 16 App Router

---

## 📋 目次

- [プロジェクト概要](#プロジェクト概要)
- [ルーティングアーキテクチャ](#ルーティングアーキテクチャ)
- [ページルート](#ページルート)
  - [公開ページ](#公開ページ)
  - [認証が必要なページ](#認証が必要なページ)
  - [管理者専用ページ](#管理者専用ページ)
  - [スーパー管理者専用ページ](#スーパー管理者専用ページ)
- [APIルート](#apiルート)
  - [認証API](#認証api)
  - [予約API](#予約api)
  - [管理者予約API](#管理者予約api)
  - [メニューAPI](#メニューapi)
  - [スタッフAPI](#スタッフapi)
  - [管理者スタッフAPI](#管理者スタッフapi)
  - [管理者顧客API](#管理者顧客api)
  - [管理者分析API](#管理者分析api)
  - [管理者設定API](#管理者設定api)
  - [機能フラグAPI](#機能フラグapi)
  - [ユーティリティAPI](#ユーティリティapi)
- [ルート命名規則](#ルート命名規則)
- [認証とアクセス制御](#認証とアクセス制御)
- [エラーハンドリング](#エラーハンドリング)

---

## 🎯 プロジェクト概要

このシステムは Next.js 16 の App Router を採用し、ファイルベースのルーティングを使用しています。

**技術スタック:**
- Next.js 16.1.1
- React 19.2.3
- TypeScript 5.x
- App Router (ファイルベースルーティング)

**ルーティング方式:**
- ページルート: `page.tsx` によるUIレンダリング
- APIルート: `route.ts` によるRESTful API
- 動的ルート: `[id]` フォルダによるパラメータ受け取り
- ルートグループ: `(group)` フォルダによる論理的なグループ化（URLには影響しない）

---

## 🏗️ ルーティングアーキテクチャ

### ディレクトリ構造

```
src/app/
├── page.tsx                              # / (ホーム)
├── login/
│   └── page.tsx                          # /login (ログイン)
├── register/
│   └── page.tsx                          # /register (ユーザー登録)
├── menus/
│   └── page.tsx                          # /menus (メニュー一覧)
├── booking/
│   └── page.tsx                          # /booking (予約作成)
├── my-reservations/
│   └── page.tsx                          # /my-reservations (マイ予約)
├── mypage/
│   └── page.tsx                          # /mypage (マイページ)
├── privacy/
│   └── page.tsx                          # /privacy (プライバシーポリシー)
├── terms/
│   └── page.tsx                          # /terms (利用規約)
├── help/
│   └── page.tsx                          # /help (ヘルプ)
├── maintenance/
│   └── page.tsx                          # /maintenance (メンテナンスモード)
├── admin/
│   ├── login/
│   │   └── page.tsx                      # /admin/login (管理者ログイン)
│   ├── dashboard/
│   │   └── page.tsx                      # /admin/dashboard (管理者ダッシュボード)
│   ├── reservations/
│   │   └── page.tsx                      # /admin/reservations (予約管理)
│   ├── customers/
│   │   ├── page.tsx                      # /admin/customers (顧客一覧)
│   │   └── [id]/page.tsx                 # /admin/customers/:id (顧客詳細)
│   ├── staff/
│   │   └── page.tsx                      # /admin/staff (スタッフ管理)
│   ├── menus/
│   │   └── page.tsx                      # /admin/menus (メニュー管理)
│   ├── blocked-times/
│   │   └── page.tsx                      # /admin/blocked-times (予約ブロック管理)
│   ├── settings/
│   │   └── page.tsx                      # /admin/settings (店舗設定)
│   └── analytics/
│       └── page.tsx                      # /admin/analytics (分析レポート)
├── super-admin/
│   ├── login/
│   │   └── page.tsx                      # /super-admin/login (スーパー管理者ログイン)
│   ├── dashboard/
│   │   └── page.tsx                      # /super-admin/dashboard (テナント管理)
│   └── feature-flags/
│       └── page.tsx                      # /super-admin/feature-flags (機能フラグ管理)
└── api/
    ├── auth/
    │   ├── register/route.ts             # POST /api/auth/register
    │   ├── login/route.ts                # POST /api/auth/login
    │   ├── logout/route.ts               # POST /api/auth/logout
    │   └── super-admin/login/route.ts    # POST /api/auth/super-admin/login
    ├── reservations/
    │   ├── route.ts                      # GET, POST /api/reservations
    │   └── [id]/route.ts                 # GET, PATCH, DELETE /api/reservations/:id
    ├── admin/
    │   ├── reservations/
    │   │   ├── route.ts                  # GET, POST /api/admin/reservations
    │   │   └── [id]/route.ts             # GET, PATCH, DELETE /api/admin/reservations/:id
    │   ├── customers/
    │   │   ├── route.ts                  # GET /api/admin/customers
    │   │   └── [id]/
    │   │       ├── route.ts              # GET, PATCH /api/admin/customers/:id
    │   │       └── memo/route.ts         # PATCH /api/admin/customers/:id/memo
    │   ├── staff/
    │   │   ├── route.ts                  # GET, POST /api/admin/staff
    │   │   └── [id]/
    │   │       ├── route.ts              # GET, PATCH, DELETE /api/admin/staff/:id
    │   │       ├── shifts/route.ts       # GET, POST /api/admin/staff/:id/shifts
    │   │       └── vacations/route.ts    # GET, POST /api/admin/staff/:id/vacations
    │   ├── menus/
    │   │   ├── route.ts                  # GET, POST /api/admin/menus
    │   │   └── [id]/route.ts             # PATCH, DELETE /api/admin/menus/:id
    │   ├── blocked-times/
    │   │   ├── route.ts                  # GET, POST /api/admin/blocked-times
    │   │   └── [id]/route.ts             # DELETE /api/admin/blocked-times/:id
    │   ├── settings/route.ts             # GET, PATCH /api/admin/settings
    │   ├── dashboard/route.ts            # GET /api/admin/dashboard
    │   ├── stats/route.ts                # GET /api/admin/stats
    │   └── analytics/
    │       ├── route.ts                  # GET /api/admin/analytics
    │       └── repeat-rate/route.ts      # GET /api/admin/analytics/repeat-rate
    ├── super-admin/
    │   └── feature-flags/route.ts        # GET, PUT /api/super-admin/feature-flags
    ├── feature-flags/route.ts            # GET /api/feature-flags (公開読み取り)
    ├── internal/
    │   └── public-status/route.ts        # GET /api/internal/public-status
    ├── cron/
    │   └── send-reminders/route.ts       # GET /api/cron/send-reminders
    ├── menus/route.ts                    # GET, POST /api/menus
    ├── staff/route.ts                    # GET, POST /api/staff
    ├── available-slots/route.ts          # GET /api/available-slots
    └── health/route.ts                   # GET /api/health
```

---

## 📄 ページルート

### 公開ページ

これらのページは認証なしでアクセス可能です。

| ルート | ファイル | 説明 | 主要機能 |
|--------|---------|------|----------|
| `/` | `app/page.tsx` | ホームページ | システム紹介、メニュー表示、予約開始ボタン |
| `/login` | `app/login/page.tsx` | ログイン | メール・パスワードでログイン |
| `/register` | `app/register/page.tsx` | ユーザー登録 | 新規アカウント作成 |
| `/menus` | `app/menus/page.tsx` | メニュー一覧 | 提供メニューの閲覧 |
| `/privacy` | `app/privacy/page.tsx` | プライバシーポリシー | 個人情報保護方針 |
| `/terms` | `app/terms/page.tsx` | 利用規約 | サービス利用規約 |
| `/help` | `app/help/page.tsx` | ヘルプ | よくある質問・使い方 |
| `/maintenance` | `app/maintenance/page.tsx` | メンテナンス | システム停止時表示 |

**認証チェック:**
- ログイン済みユーザーが `/login` または `/register` にアクセスした場合、ホームページにリダイレクト

---

### 認証が必要なページ

これらのページはログイン済みユーザーのみアクセス可能です。

| ルート | ファイル | 説明 | 主要機能 |
|--------|---------|------|----------|
| `/booking` | `app/booking/page.tsx` | 予約作成 | メニュー選択、日時選択、スタッフ選択、予約確定 |
| `/my-reservations` | `app/my-reservations/page.tsx` | マイ予約一覧 | 自分の予約を表示、キャンセル機能 |
| `/mypage` | `app/mypage/page.tsx` | マイページ | 来店履歴・予約一覧 |

**認証チェック:**
- 未ログインユーザーがアクセスした場合、`/login` にリダイレクト

---

### 管理者専用ページ（ADMIN）

これらのページは管理者権限（ADMIN/SUPER_ADMIN）を持つユーザーのみアクセス可能です。

| ルート | ファイル | 説明 | 主要機能 |
|--------|---------|------|----------|
| `/admin/login` | `app/admin/login/page.tsx` | 管理者ログイン | 管理者専用ログイン画面 |
| `/admin/dashboard` | `app/admin/dashboard/page.tsx` | ダッシュボード | 統計情報表示（今日の予約、月間売上、リピート率など） |
| `/admin/reservations` | `app/admin/reservations/page.tsx` | 予約管理 | 全予約の表示・検索・フィルタリング、ステータス変更 |
| `/admin/customers` | `app/admin/customers/page.tsx` | 顧客一覧 | 顧客情報の検索・表示 |
| `/admin/customers/[id]` | `app/admin/customers/[id]/page.tsx` | 顧客詳細 | 顧客詳細・来店履歴・メモ編集 |
| `/admin/staff` | `app/admin/staff/page.tsx` | スタッフ管理 | スタッフの追加・編集・削除、シフト管理 |
| `/admin/menus` | `app/admin/menus/page.tsx` | メニュー管理 | メニューのCRUD操作 |
| `/admin/blocked-times` | `app/admin/blocked-times/page.tsx` | 予約ブロック管理 | 予約不可時間帯の設定 |
| `/admin/settings` | `app/admin/settings/page.tsx` | 店舗設定 | 営業時間・予約設定・基本情報 |
| `/admin/analytics` | `app/admin/analytics/page.tsx` | 分析レポート | 予約推移グラフ・リピート率分析 |

**認証チェック:**
- 未ログインユーザーがアクセスした場合、`/admin/login` にリダイレクト
- 一般ユーザー（CUSTOMER）がアクセスした場合、ホームページにリダイレクト

**管理者判定:**
- `lib/auth.ts` の `isAdmin()` 関数で判定
- ユーザーの `role` が `ADMIN` または `SUPER_ADMIN` の場合に管理者権限

---

### スーパー管理者専用ページ（SUPER_ADMIN）

これらのページはスーパー管理者（開発者）のみアクセス可能です。

| ルート | ファイル | 説明 | 主要機能 |
|--------|---------|------|----------|
| `/super-admin/login` | `app/super-admin/login/page.tsx` | スーパー管理者ログイン | 開発者専用ログイン画面 |
| `/super-admin/dashboard` | `app/super-admin/dashboard/page.tsx` | テナント管理 | 全テナントの状況確認 |
| `/super-admin/feature-flags` | `app/super-admin/feature-flags/page.tsx` | 機能フラグ管理 | オプション機能のON/OFF制御 |

**認証チェック:**
- 未ログインユーザーがアクセスした場合、`/super-admin/login` にリダイレクト
- ADMIN以下の権限でアクセスした場合、ホームページにリダイレクト

**スーパー管理者判定:**
- ユーザーの `role` が `SUPER_ADMIN` の場合のみアクセス可能
- 詳細は `documents/spec/ロール設計書.md` を参照

---

## 🔌 APIルート

### 認証API

| メソッド | エンドポイント | 説明 | リクエスト | レスポンス |
|---------|--------------|------|-----------|-----------|
| **POST** | `/api/auth/register` | ユーザー登録 | `{ name, email, phone, password, passwordConfirm, termsAccepted }` | `201: { id, name, email }` |
| **POST** | `/api/auth/login` | ログイン | `{ email, password, remember? }` | `200: { user: { id, name, email } }` |
| **POST** | `/api/auth/logout` | ログアウト | - | `200: { message }` |
| **POST** | `/api/auth/super-admin/login` | スーパー管理者ログイン | `{ email, password }` | `200: { user: { id, name, email, role } }` |

**実装ファイル:**
- `app/api/auth/register/route.ts`
- `app/api/auth/login/route.ts`
- `app/api/auth/logout/route.ts`
- `app/api/auth/super-admin/login/route.ts`

**認証方式:**
- Supabase Auth を使用
- Cookie ベースのセッション管理
- レート制限適用（ログイン: 10回/分、登録: 5回/時間）

---

### 予約API

ユーザーが自分の予約を管理するためのAPI。

| メソッド | エンドポイント | 認証 | 説明 |
|---------|--------------|------|------|
| **GET** | `/api/reservations` | 必須 | 自分の予約一覧取得 |
| **POST** | `/api/reservations` | 必須 | 新規予約作成 |
| **GET** | `/api/reservations/:id` | 必須 | 予約詳細取得 |
| **PATCH** | `/api/reservations/:id` | 必須 | 予約内容変更 |
| **DELETE** | `/api/reservations/:id` | 必須 | 予約キャンセル |

**実装ファイル:**
- `app/api/reservations/route.ts` (GET, POST)
- `app/api/reservations/[id]/route.ts` (GET, PATCH, DELETE)

**アクセス制御:**
- 自分の予約のみ取得・変更・削除可能（`userId` でフィルタリング）

---

### 管理者予約API

管理者が全ユーザーの予約を管理するためのAPI。

| メソッド | エンドポイント | 認証 | 説明 |
|---------|--------------|------|------|
| **GET** | `/api/admin/reservations` | 管理者 | 全予約取得（フィルタリング・検索機能付き） |
| **POST** | `/api/admin/reservations` | 管理者 | 予約代行作成 |
| **GET** | `/api/admin/reservations/:id` | 管理者 | 予約詳細取得 |
| **PATCH** | `/api/admin/reservations/:id` | 管理者 | 予約情報更新（ステータス変更含む） |
| **DELETE** | `/api/admin/reservations/:id` | 管理者 | 予約削除 |

**実装ファイル:**
- `app/api/admin/reservations/route.ts` (GET, POST)
- `app/api/admin/reservations/[id]/route.ts` (GET, PATCH, DELETE)

**フィルタリングオプション (GET):**
- `status`: 予約ステータス ('all', 'PENDING', 'CONFIRMED', 'CANCELLED', 'COMPLETED', 'NO_SHOW')
- `dateRange`: 日付範囲 ('all', 'this-week', 'this-month')
- `search`: 顧客名で検索

---

### メニューAPI

| メソッド | エンドポイント | 認証 | 説明 |
|---------|--------------|------|------|
| **GET** | `/api/menus` | 不要 | メニュー一覧取得 |
| **POST** | `/api/menus` | 管理者 | メニュー作成 |

**実装ファイル:**
- `app/api/menus/route.ts`

---

### スタッフAPI

| メソッド | エンドポイント | 認証 | 説明 |
|---------|--------------|------|------|
| **GET** | `/api/staff` | 不要 | アクティブなスタッフ一覧取得 |
| **POST** | `/api/staff` | 管理者 | スタッフ作成 |

**実装ファイル:**
- `app/api/staff/route.ts`

---

### 管理者スタッフAPI

管理者がスタッフ情報を詳細に管理するためのAPI。

| メソッド | エンドポイント | 認証 | 説明 |
|---------|--------------|------|------|
| **GET** | `/api/admin/staff` | 管理者 | スタッフ一覧取得（予約件数含む） |
| **POST** | `/api/admin/staff` | 管理者 | スタッフ作成 |
| **GET** | `/api/admin/staff/:id` | 管理者 | スタッフ詳細取得 |
| **PATCH** | `/api/admin/staff/:id` | 管理者 | スタッフ情報更新 |
| **DELETE** | `/api/admin/staff/:id` | 管理者 | スタッフ削除（論理削除） |
| **GET** | `/api/admin/staff/:id/shifts` | 管理者 | スタッフシフト取得 |
| **POST** | `/api/admin/staff/:id/shifts` | 管理者 | スタッフシフト作成 |
| **GET** | `/api/admin/staff/:id/vacations` | 管理者 | スタッフ休暇取得 |
| **POST** | `/api/admin/staff/:id/vacations` | 管理者 | スタッフ休暇登録 |

**実装ファイル:**
- `app/api/admin/staff/route.ts` (GET, POST)
- `app/api/admin/staff/[id]/route.ts` (GET, PATCH, DELETE)
- `app/api/admin/staff/[id]/shifts/route.ts` (GET, POST)
- `app/api/admin/staff/[id]/vacations/route.ts` (GET, POST)

---

### 管理者統計API

| メソッド | エンドポイント | 認証 | 説明 |
|---------|--------------|------|------|
| **GET** | `/api/admin/stats` | 管理者 | ダッシュボード統計情報取得 |
| **GET** | `/api/admin/dashboard` | 管理者 | ダッシュボードデータ取得 |

**実装ファイル:**
- `app/api/admin/stats/route.ts`
- `app/api/admin/dashboard/route.ts`

**レスポンス内容:**
- 今日の予約件数
- 月間予約件数
- 月間売上
- リピート率
- 今日の予約一覧
- 過去7日間の予約推移

---

### 管理者顧客API

管理者が顧客情報を管理するためのAPI。

| メソッド | エンドポイント | 認証 | 説明 |
|---------|--------------|------|------|
| **GET** | `/api/admin/customers` | 管理者 | 顧客一覧取得（検索・ソート対応） |
| **GET** | `/api/admin/customers/:id` | 管理者 | 顧客詳細・来店履歴取得 |
| **PATCH** | `/api/admin/customers/:id` | 管理者 | 顧客情報更新 |
| **PATCH** | `/api/admin/customers/:id/memo` | 管理者 | 顧客メモ更新 |

**実装ファイル:**
- `app/api/admin/customers/route.ts` (GET)
- `app/api/admin/customers/[id]/route.ts` (GET, PATCH)
- `app/api/admin/customers/[id]/memo/route.ts` (PATCH)

---

### 管理者分析API

分析レポート機能のためのAPI。

| メソッド | エンドポイント | 認証 | 説明 |
|---------|--------------|------|------|
| **GET** | `/api/admin/analytics` | 管理者 | 予約推移データ取得（日別/週別/月別） |
| **GET** | `/api/admin/analytics/repeat-rate` | 管理者 | リピート率分析データ取得 |

**実装ファイル:**
- `app/api/admin/analytics/route.ts`
- `app/api/admin/analytics/repeat-rate/route.ts`

---

### 管理者メニューAPI

管理者がメニューを管理するためのAPI。

| メソッド | エンドポイント | 認証 | 説明 |
|---------|--------------|------|------|
| **GET** | `/api/admin/menus` | 管理者 | メニュー一覧取得（無効含む） |
| **POST** | `/api/admin/menus` | 管理者 | メニュー作成 |
| **PATCH** | `/api/admin/menus/:id` | 管理者 | メニュー更新 |
| **DELETE** | `/api/admin/menus/:id` | 管理者 | メニュー削除（論理削除） |

**実装ファイル:**
- `app/api/admin/menus/route.ts` (GET, POST)
- `app/api/admin/menus/[id]/route.ts` (PATCH, DELETE)

---

### 管理者予約ブロックAPI

予約不可時間帯を管理するためのAPI。

| メソッド | エンドポイント | 認証 | 説明 |
|---------|--------------|------|------|
| **GET** | `/api/admin/blocked-times` | 管理者 | ブロック時間帯一覧取得 |
| **POST** | `/api/admin/blocked-times` | 管理者 | ブロック時間帯作成 |
| **DELETE** | `/api/admin/blocked-times/:id` | 管理者 | ブロック時間帯削除 |

**実装ファイル:**
- `app/api/admin/blocked-times/route.ts` (GET, POST)
- `app/api/admin/blocked-times/[id]/route.ts` (DELETE)

---

### 管理者設定API

店舗設定を管理するためのAPI。

| メソッド | エンドポイント | 認証 | 説明 |
|---------|--------------|------|------|
| **GET** | `/api/admin/settings` | 管理者 | 店舗設定取得 |
| **PATCH** | `/api/admin/settings` | 管理者 | 店舗設定更新 |

**実装ファイル:**
- `app/api/admin/settings/route.ts` (GET, PATCH)

**設定項目:**
- 営業時間（開店・閉店時刻）
- 定休日
- 予約枠間隔
- 予約受付期間（最小・最大日数）
- キャンセル期限
- システム公開/非公開フラグ

---

### 機能フラグAPI

オプション機能のON/OFF管理API。

| メソッド | エンドポイント | 認証 | 説明 |
|---------|--------------|------|------|
| **GET** | `/api/feature-flags` | 不要 | 機能フラグ取得（公開用） |
| **GET** | `/api/super-admin/feature-flags` | スーパー管理者 | 機能フラグ詳細取得 |
| **PUT** | `/api/super-admin/feature-flags` | スーパー管理者 | 機能フラグ更新 |

**実装ファイル:**
- `app/api/feature-flags/route.ts` (GET)
- `app/api/super-admin/feature-flags/route.ts` (GET, PUT)

**機能フラグ一覧:**
- `enableStaffSelection` - スタッフ指名機能
- `enableStaffShiftManagement` - スタッフシフト管理
- `enableCustomerManagement` - 顧客管理・メモ機能
- `enableReservationUpdate` - 予約変更機能
- `enableReminderEmail` - リマインダーメール
- `enableManualReservation` - 予約手動追加
- `enableAnalyticsReport` - 分析レポート
- `enableRepeatRateAnalysis` - リピート率分析
- `enableCouponFeature` - クーポン機能
- `enableLineNotification` - LINE通知連携

---

### ユーティリティAPI

| メソッド | エンドポイント | 認証 | 説明 |
|---------|--------------|------|------|
| **GET** | `/api/available-slots` | 不要 | 空き時間スロット取得 |
| **GET** | `/api/health` | 不要 | ヘルスチェック（DB接続確認） |
| **GET** | `/api/cron/send-reminders` | CRON_SECRET | リマインダーメール送信（定期実行） |
| **GET** | `/api/internal/public-status` | 不要 | システム公開状態確認 |

**実装ファイル:**
- `app/api/available-slots/route.ts`
- `app/api/health/route.ts`
- `app/api/cron/send-reminders/route.ts`
- `app/api/internal/public-status/route.ts`

**available-slots パラメータ:**
- `date`: 予約日 (YYYY-MM-DD)
- `menuId`: メニューID (UUID)
- `staffId`: スタッフID (UUID, オプション)

**cron/send-reminders:**
- 認証: `CRON_SECRET` ヘッダーで検証
- 用途: Vercel Cronから定期実行（前日リマインダー送信）

**internal/public-status:**
- 用途: メンテナンスモード判定に使用

---

## 📝 ルート命名規則

### ページルート

- **単数形**: `/booking`, `/mypage` (単一リソースまたはアクション)
- **複数形**: `/menus`, `/my-reservations` (複数リソースの一覧)
- **動詞**: `/login`, `/register` (アクション)

### APIルート

- **RESTful原則に従う**:
  - リソースは複数形: `/api/reservations`, `/api/menus`
  - 動的パラメータ: `[id]` フォルダで表現
  - ネストされたリソース: `/api/admin/staff/:id/shifts`

### ルートグループ

現在このプロジェクトではルートグループ `(auth)`, `(booking)`, `(admin)` は使用していません。
将来的にレイアウトを分けたい場合は、以下のように使用可能です:

```
app/
├── (auth)/
│   ├── login/page.tsx           # /login
│   └── register/page.tsx        # /register
├── (booking)/
│   ├── menus/page.tsx           # /menus
│   └── booking/page.tsx         # /booking
└── (admin)/
    └── admin/
        └── dashboard/page.tsx   # /admin/dashboard
```

ルートグループはURLには影響せず、レイアウトの共有やコード整理に利用されます。

---

## 🔐 認証とアクセス制御

### 認証レベル

| レベル | 説明 | 実装方法 |
|-------|------|----------|
| **公開** | 誰でもアクセス可能 | アクセス制限なし |
| **認証必須** | ログインユーザーのみ | `requireAuth()` で認証チェック |
| **管理者のみ** | 管理者権限が必要 | `requireAdmin()` で管理者チェック |

### 実装例

**ページでの認証チェック (app/booking/page.tsx):**

```typescript
import { requireAuth } from '@/lib/auth';

export default async function BookingPage() {
  const user = await requireAuth();

  return <BookingForm user={user} />;
}
```

**APIでの認証チェック (app/api/reservations/route.ts):**

```typescript
import { getCurrentUser } from '@/lib/auth';

export async function GET(request: Request) {
  const user = await getCurrentUser();

  if (!user) {
    return errorResponse('Unauthorized', 401, 'UNAUTHORIZED');
  }

  // ...
}
```

**管理者チェック (app/api/admin/stats/route.ts):**

```typescript
import { requireAdmin } from '@/lib/auth';

export async function GET(request: Request) {
  await requireAdmin();

  // 管理者のみ実行可能な処理
}
```

---

## 🚨 エラーハンドリング

### ページルートのエラーハンドリング

- **認証エラー**: `/login` にリダイレクト
- **権限エラー**: ホームページにリダイレクト
- **その他のエラー**: エラーページ表示

### APIルートのエラーハンドリング

統一されたエラーレスポンス形式を使用:

```typescript
{
  "error": "Error message",
  "code": "ERROR_CODE",
  "statusCode": 400
}
```

**主なエラーコード:**
- `UNAUTHORIZED` (401): 認証エラー
- `FORBIDDEN` (403): 権限エラー
- `NOT_FOUND` (404): リソースが存在しない
- `VALIDATION_ERROR` (400): バリデーションエラー
- `CONFLICT` (409): リソースの競合
- `INTERNAL_ERROR` (500): サーバーエラー

**エラーハンドリングヘルパー:**
- `lib/api-response.ts` に統一されたレスポンス関数を実装
  - `successResponse(data, status)`
  - `errorResponse(message, status, code, details?)`

---

## 📊 ルート一覧サマリー

### ページルート (22件)

#### 公開ページ (8件)
| # | ルート | 認証 | 説明 |
|---|--------|------|------|
| 1 | `/` | 不要 | ホームページ |
| 2 | `/login` | 不要 | ログイン |
| 3 | `/register` | 不要 | ユーザー登録 |
| 4 | `/menus` | 不要 | メニュー一覧 |
| 5 | `/privacy` | 不要 | プライバシーポリシー |
| 6 | `/terms` | 不要 | 利用規約 |
| 7 | `/help` | 不要 | ヘルプ |
| 8 | `/maintenance` | 不要 | メンテナンスモード |

#### 認証必須ページ (3件)
| # | ルート | 認証 | 説明 |
|---|--------|------|------|
| 9 | `/booking` | 必須 | 予約作成 |
| 10 | `/my-reservations` | 必須 | マイ予約 |
| 11 | `/mypage` | 必須 | マイページ |

#### 管理者ページ (8件)
| # | ルート | 認証 | 説明 |
|---|--------|------|------|
| 12 | `/admin/login` | 不要 | 管理者ログイン |
| 13 | `/admin/dashboard` | 管理者 | ダッシュボード |
| 14 | `/admin/reservations` | 管理者 | 予約管理 |
| 15 | `/admin/customers` | 管理者 | 顧客一覧 |
| 16 | `/admin/customers/[id]` | 管理者 | 顧客詳細 |
| 17 | `/admin/staff` | 管理者 | スタッフ管理 |
| 18 | `/admin/menus` | 管理者 | メニュー管理 |
| 19 | `/admin/blocked-times` | 管理者 | 予約ブロック管理 |
| 20 | `/admin/settings` | 管理者 | 店舗設定 |
| 21 | `/admin/analytics` | 管理者 | 分析レポート |

#### スーパー管理者ページ (3件)
| # | ルート | 認証 | 説明 |
|---|--------|------|------|
| 22 | `/super-admin/login` | 不要 | スーパー管理者ログイン |
| 23 | `/super-admin/dashboard` | スーパー管理者 | テナント管理 |
| 24 | `/super-admin/feature-flags` | スーパー管理者 | 機能フラグ管理 |

### APIルート (33件)

#### 認証API (4件)
| # | エンドポイント | メソッド | 認証 | 説明 |
|---|--------------|---------|------|------|
| 1 | `/api/auth/register` | POST | 不要 | ユーザー登録 |
| 2 | `/api/auth/login` | POST | 不要 | ログイン |
| 3 | `/api/auth/logout` | POST | 不要 | ログアウト |
| 4 | `/api/auth/super-admin/login` | POST | 不要 | スーパー管理者ログイン |

#### 予約API (5件)
| # | エンドポイント | メソッド | 認証 | 説明 |
|---|--------------|---------|------|------|
| 5 | `/api/reservations` | GET, POST | 必須 | 予約一覧・作成 |
| 6 | `/api/reservations/:id` | GET, PATCH, DELETE | 必須 | 予約詳細・更新・削除 |
| 7 | `/api/admin/reservations` | GET, POST | 管理者 | 全予約一覧・作成 |
| 8 | `/api/admin/reservations/:id` | GET, PATCH, DELETE | 管理者 | 予約詳細・更新・削除 |

#### メニュー・スタッフAPI (9件)
| # | エンドポイント | メソッド | 認証 | 説明 |
|---|--------------|---------|------|------|
| 9 | `/api/menus` | GET, POST | POST:管理者 | メニュー一覧・作成 |
| 10 | `/api/staff` | GET, POST | POST:管理者 | スタッフ一覧・作成 |
| 11 | `/api/admin/menus` | GET, POST | 管理者 | メニュー管理 |
| 12 | `/api/admin/menus/:id` | PATCH, DELETE | 管理者 | メニュー編集・削除 |
| 13 | `/api/admin/staff` | GET, POST | 管理者 | スタッフ一覧・作成 |
| 14 | `/api/admin/staff/:id` | GET, PATCH, DELETE | 管理者 | スタッフ詳細・更新・削除 |
| 15 | `/api/admin/staff/:id/shifts` | GET, POST | 管理者 | シフト取得・作成 |
| 16 | `/api/admin/staff/:id/vacations` | GET, POST | 管理者 | 休暇取得・作成 |

#### 顧客・分析API (6件)
| # | エンドポイント | メソッド | 認証 | 説明 |
|---|--------------|---------|------|------|
| 17 | `/api/admin/customers` | GET | 管理者 | 顧客一覧 |
| 18 | `/api/admin/customers/:id` | GET, PATCH | 管理者 | 顧客詳細・更新 |
| 19 | `/api/admin/customers/:id/memo` | PATCH | 管理者 | 顧客メモ更新 |
| 20 | `/api/admin/dashboard` | GET | 管理者 | ダッシュボードデータ |
| 21 | `/api/admin/stats` | GET | 管理者 | 統計情報取得 |
| 22 | `/api/admin/analytics` | GET | 管理者 | 予約推移データ |
| 23 | `/api/admin/analytics/repeat-rate` | GET | 管理者 | リピート率分析 |

#### 設定・ブロックAPI (4件)
| # | エンドポイント | メソッド | 認証 | 説明 |
|---|--------------|---------|------|------|
| 24 | `/api/admin/settings` | GET, PATCH | 管理者 | 店舗設定 |
| 25 | `/api/admin/blocked-times` | GET, POST | 管理者 | ブロック時間帯一覧・作成 |
| 26 | `/api/admin/blocked-times/:id` | DELETE | 管理者 | ブロック時間帯削除 |

#### 機能フラグAPI (2件)
| # | エンドポイント | メソッド | 認証 | 説明 |
|---|--------------|---------|------|------|
| 27 | `/api/feature-flags` | GET | 不要 | 機能フラグ取得（公開） |
| 28 | `/api/super-admin/feature-flags` | GET, PUT | スーパー管理者 | 機能フラグ管理 |

#### ユーティリティAPI (4件)
| # | エンドポイント | メソッド | 認証 | 説明 |
|---|--------------|---------|------|------|
| 29 | `/api/available-slots` | GET | 不要 | 空き時間取得 |
| 30 | `/api/health` | GET | 不要 | ヘルスチェック |
| 31 | `/api/cron/send-reminders` | GET | CRON_SECRET | リマインダー送信 |
| 32 | `/api/internal/public-status` | GET | 不要 | 公開状態確認 |

**合計**: 56ルート（ページ24 + API32）

---

## 📚 関連ドキュメント

- **API設計書**: `documents/api/API設計書.md`
- **データベース設計書**: `documents/spec/データベース設計書.md`
- **プロジェクト概要**: `documents/basic/プロジェクト概要.md`
- **機能一覧とページ設計**: `documents/basic/機能一覧とページ設計.md`

---

**最終更新**: 2026-01-04
**管理者**: Claude Code
